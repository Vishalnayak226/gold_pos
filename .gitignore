node_modules/
.env
*.log
backend/logs/
backend/backups/
backend/data/
backend/_rollback/
backend/_staging/
# Default output of `npm run seed` — a generated fixture database, regenerable
# byte-for-byte from backend/seed.js, so there is nothing to track.
backend/data-seed/
# Playwright artefacts (traces, screenshots, HTML report)
backend/test-results/
backend/playwright-report/
dist/
gold_pos_release.zip
backups/
licensing_server/data/
graphify-out/
skill-observations/

# Agent scratch dirs occasionally materialised in the repo root by mistake
UsersABCDAppDataLocalTemp*/

# Private keys and offline developer-only material must never be committed
developer_doomsday_keys/*.pem
developer_blackbox_keys/*.pem
licensing_server/keys/*private*.pem
docs/credentials.md
