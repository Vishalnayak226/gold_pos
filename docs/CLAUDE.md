# Moved

The graphify rules and documentation conventions that used to live here are now in the root
`CLAUDE.md` (§4 Documentation conventions, §5 Context discipline → graphify), which is
auto-loaded every turn. The reasoning behind them is in `docs/FOUNDATION.md`.

This stub exists only so a session working inside `docs/` is not silently loading a second,
diverging copy of the rules. Do not add rules here.
