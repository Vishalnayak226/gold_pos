# Project Ledger & Change Log

This file tracks all modifications, additions, and updates made to the Gold Business POS project. Every development phase and change must be logged here.

---

### Ledger Entries


**Phases 1–18 (2026-07-12 → 2026-07-17) — closed.** Archived → `docs/archive/ledger_closed_phases.md`

| Date | Time (IST) | Author | Task / Change Description | Files Modified |
| :--- | :--- | :--- | :--- | :--- |
| 2026-08-24 | -- | AI Assistant | **Phase 44: catalogue-to-billing, atomic stock movements, return exchange/void, off-site destination and defined management reports**<br>- **SKU/lot billing:** Billing Desk resolves a barcode/SKU to catalogue name, purity and nominal weight, then requires an exact on-hand lot. The server revalidates active item, branch, purity and balance under the sale transaction and records item/lot references on the invoice line.<br>- **Atomic stock:** additive migration 016 keeps the original opening/adjustment ledger intact and adds a trigger-enforced append-only document-movement ledger. Sale decrements, return restores and same-day void reverses the exact lot in the same transaction as the invoice/credit note; negative stock rolls the whole sale back. Lots can carry optional cost/gram for reporting.<br>- **Exchange and void:** exchange returns file a marked customer credit, restore linked stock and bind once to a replacement invoice. Owner/manager-only void requires a reason, current business date, issued state and no return; it preserves the invoice/tenders and appends stock and redeemed-advance reversals before marking the invoice cancelled.<br>- **Off-site destination:** nightly/manual backups optionally copy to an env/configured mounted or synchronised directory, verify every file by SHA-256, write a manifest, prune by independent retention and alert on failure. Cloud/NAS credentials remain outside the POS. The archive as a whole is not encrypted and a real remote destination/drill remain deployment work.<br>- **Reports:** new Management Reports screen and repository-owned Settlement, Reconciliation, Gross Profitability and Inventory Ageing endpoints. Definitions travel in every response: counter tenders less cash-like refunds (voids separate); invoice counter-payable/gateway-credit exceptions; net-of-GST remaining revenue less linked-lot cost with cost coverage; positive stock bucketed by lot-open age. They are operational views, not statutory labels.<br>- **Verification:** 16/16 additive migrations safe; full `npm test` green across all nine suites (164 billing, 81 schema, 150 repository/service/importer, 16 concurrency, 14 integration blocks, 29 route-boundary, 115 live HTTP, 17 production-guard and 16 alerting checks); isolated off-site copy/manifest/retention/path-guard test green; syntax checks green; `npm audit` 0 vulnerabilities; Playwright 44/44 desktop/mobile journeys green. | `backend/repositories/migrations/016_inventory_billing_and_reports.sql`<br>`backend/repositories/reportRepository.js`<br>`backend/repositories/{index,inventoryRepository,invoiceRepository,creditNoteRepository}.js`<br>`backend/services/{saleService,returnService}.js`<br>`backend/{server,backupEngine,defaultSettings,test_repositories,test_http,test_suite}.js`<br>`backend/tests/e2e/inventory-billing-operations.spec.js`<br>`frontend/js/components/{BillingDesk,ReturnDesk,ReprintDesk,InventoryManager,ReportsDesk,SettingsManager}.js`<br>`frontend/js/app.js`<br>`frontend/index.html`<br>`docs/{PRODUCTION_READINESS_ROADMAP,TESTING_CHECKLIST,ai_handover,LEDGER}.md` |
| 2026-07-17 | -- | AI Assistant | **Phase 19: Owner's internal Dev/Sandbox/Live promotion pipeline**<br>- Added `GET /api/health` (public, license-gate-exempt) to both `backend/server.js` and `licensing_server/server.js`, reporting `{status, version, env}` for post-deploy smoke tests.<br>- `deploy/ecosystem.base.cjs` factory + 5 per-environment PM2 configs so Dev/Sandbox/Live plus non-prod/production licensing servers coexist as distinct PM2 apps on one pipeline VPS; existing single-tenant `deploy/ecosystem.config.cjs` untouched.<br>- `deploy/remote-deploy.sh` — one shared, parameterized deploy script (git reset --hard + npm ci + pm2 startOrRestart + pm2 save) used identically by hand or from CI.<br>- Three GitHub Actions workflows (`cd-dev.yml`, `cd-sandbox.yml`, `cd-live.yml`): test+audit gate → SSH deploy via `remote-deploy.sh` → `/api/health` smoke test. `cd-live.yml` requires a GitHub Environment manual-approval click before touching the platform owner's own Live instance — this is separate from, and does not change, the per-tenant manual/auto-update process in Phase 18.<br>- `deploy/README.md` §8 documents the full layout table, one-time provisioning steps, and day-to-day promotion flow (`feature branch → develop → staging → main`). Not yet exercised end-to-end — no VPS/domain provisioned yet. | - [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [licensing_server/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/licensing_server/server.js)<br>- [deploy/ecosystem.base.cjs](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/deploy/ecosystem.base.cjs)<br>- [deploy/remote-deploy.sh](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/deploy/remote-deploy.sh)<br>- [.github/workflows/cd-dev.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-dev.yml)<br>- [.github/workflows/cd-sandbox.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-sandbox.yml)<br>- [.github/workflows/cd-live.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-live.yml)<br>- [deploy/README.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/deploy/README.md)<br>- [docs/GO_LIVE_CHECKLIST.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/GO_LIVE_CHECKLIST.md)<br>- [docs/PROJECT_PLAN.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/PROJECT_PLAN.md) |
| 2026-08-07 | -- | AI Assistant | **Phase 20: Settings-schema plumbing, server-side invoice authority, and the v1.2.0 release cut**<br>- **New settings keys reached nobody.** `taxMode` and `defaultDiscountPercent` were read by `BillingDesk.js` and written by `SettingsManager.js` but were in neither settings template, so fresh installs and every existing tenant read `undefined` until someone opened Settings and pressed Save. Root cause was the template being hand-maintained in two drifting copies — extracted to a single canonical `backend/defaultSettings.js` that `db.js` and `release_pipeline.js` both derive from (the release copy had also silently been missing `invoicePrefix`, `invoiceSeqStart`, `adminPin`, and `upiId`).<br>- **Added `migrateSettings()`** (`db.js`, runs on every boot): fills in template keys added since a tenant's install, deletes retired keys, normalizes narrowed enums, merges nested blocks (`smtp`, `overrideGoldPrice`) one level down, and only rewrites the file when something actually changed. Verified against a simulated pre-1.2.0 tenant carrying real business data (custom prefix `RJ`, sequence 1487, live Razorpay key, configured SMTP, manual price overrides): 14/14 checks — new keys added, `goldApiKey` removed, legacy provider normalized, every existing value preserved byte-for-byte, and idempotent on a second boot.<br>- **Removed the dead paid gold-API code.** The UI had dropped GoldAPI.io/Metals.dev and the API-key field, but `priceEngine.js` still branched on both with `settings.goldApiKey` and both templates still seeded the key. Branches and key deleted; a tenant carrying a legacy provider value is normalized to `public` rather than falling through to *mocked* prices, which is what the old `else` branch would have done.<br>- **`POST /api/sales` no longer trusts the client's `totalAmount`.** It validated the number's shape but never recomputed it, so the whole inclusive/exclusive-tax and percentage-discount pipeline lived only in the browser and a stale tab could file a wrong total to the permanent ledger. The route now recomputes every money field via the shared `frontend/js/lib/billingMath.js` using the *server's* tax slab and tax mode, persists paise-rounded server values, logs `SALE_TOTAL_MISMATCH` on divergence, and returns `totalCorrected` so the Billing Desk tells the cashier to reprint. `makingChargeAmount` and `discountPercent` gained the validation the other numeric fields already had. Admin-gated, so this is data integrity, not a closed attack surface.<br>- **Fixed `test.bat`**, which killed whatever held port 5000, printed "Server has stopped unexpectedly", and exited without starting anything. It now runs both suites and propagates a non-zero exit code.<br>- **Verification:** `node --check` on all 8 touched files; both suites green (46 billing checks, 4 integration blocks); live server smoke test on a backed-up copy of `backend/data/` covering a lying client (total 1 → 83,430), an honest client (no correction), discount-before-tax ordering (tax 2,187 on the discounted base, not 2,430), advance capped at the bill with the total floored at 0, rejection of a `-9e99` making charge and a 150% discount, and a mid-session Exclusive→Inclusive settings flip correcting a stale client total to 81,000 with 2,359.22 carved out. `backend/data/` restored and diffed identical afterward. Release bundle rebuilt and confirmed to contain the new template keys and the shared math module, with the cross-tree import resolving inside `dist/`.<br>- **Paperwork:** cut `[Unreleased]` → `[1.2.0] — 2026-08-07` in CHANGELOG.md (also relocating a Phase 19 entry that had been filed under a `Fixed` heading), bumped `backend/package.json` to 1.2.0, and logged this entry. | - [backend/defaultSettings.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/defaultSettings.js)<br>- [backend/db.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/db.js)<br>- [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/priceEngine.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/priceEngine.js)<br>- [backend/package.json](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/package.json)<br>- [frontend/js/components/BillingDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/BillingDesk.js)<br>- [release_pipeline.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/release_pipeline.js)<br>- [test.bat](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/test.bat)<br>- [CHANGELOG.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CHANGELOG.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md) |
| 2026-08-08 | -- | AI Assistant | **Phase 20.1: Customer identity & authentication (the scheme module's blocking prerequisite)**<br>- Closed the portal's open door: typing any 10-digit number into `customer.html` used to return that customer's balance, full deposit history, and the ability to deposit against their account, with no credential of any kind. New `backend/customerAuth.js` — scrypt password hashing (built-in `crypto`, no new dependency), bearer sessions persisted as SHA-256 hashes, per-account lockout persisted across restarts plus an in-memory per-IP credential-stuffing cooldown, and single-use emailed reset codes.<br>- New `/api/customer/*` surface (register, login, logout, me, profile PATCH, password change/forgot/reset, own-ledger read + deposit) with `requireCustomerSession` / `requireEstablishedCustomer`. Every customer route is scoped to the phone on the session; a `customerPhone` in the request body is ignored.<br>- Gated four previously-public endpoints: `GET /api/advances/lookup` and `POST /api/advances` to admin (they can name any customer), `POST /api/payment/order` and `/verify` to a customer session. Moved the two admin components that were calling admin-gated routes with a raw `fetch()` (`BillingDesk` advance lookup, `AdvancesManager` counter deposit) onto `adminFetch()`.<br>- Store-side path for existing customers: admin `POST /api/customer-accounts/issue-login` hands out a one-time temporary password and forces a change before any data loads; self-service registration is refused for a number that already has store history, which is what stops an outsider claiming an existing customer's ledger without an SMS gateway.<br>- Also fixed while in the payment path: a replayed `razorpay_payment_id` no longer creates a second deposit row.<br>- **Verified live, not asserted:** 53-check API pass, 20-check post-restart pass, 29-check Playwright pass at a 390px viewport — all green, `backend/data/` backed up first and diffed byte-identical afterwards. The password-reset email was captured with a throwaway local SMTP server so the code under test is the one actually delivered. One test-authored assumption was wrong (a 60s lockout had simply expired across the restart) and was re-proved with a tight-window restart instead of being explained away.<br>- Full detail: `docs/SCHEME_MODULE_PLAN.md` §20.1 (build checklist, exit criteria) and `CHANGELOG.md` [Unreleased]. Manual steps: `docs/TESTING_CHECKLIST.md` §12. | - [backend/customerAuth.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/customerAuth.js)<br>- [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/db.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/db.js)<br>- [backend/emailReporter.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/emailReporter.js)<br>- [backend/test_suite.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_suite.js)<br>- [frontend/customer.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/customer.html)<br>- [frontend/js/components/BillingDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/BillingDesk.js)<br>- [frontend/js/components/AdvancesManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/AdvancesManager.js)<br>- [CHANGELOG.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CHANGELOG.md)<br>- [docs/SCHEME_MODULE_PLAN.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/SCHEME_MODULE_PLAN.md)<br>- [docs/TESTING_CHECKLIST.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/TESTING_CHECKLIST.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/PRODUCTION_READINESS_ROADMAP.md)<br>- [docs/PROJECT_PLAN.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/PROJECT_PLAN.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md) |
| 2026-08-08 | -- | AI Assistant | **Phase 21: Credential redaction, dependency bumps, and the first HTTP route suite** (audit findings 8–10). *Numbered 21, not 20.2 — `SCHEME_MODULE_PLAN.md` §4 already reserves 20.2–20.5 for the scheme module.*<br>- **Secrets stopped reaching the browser.** `GET /api/settings` returned `settings.json` verbatim, so the Razorpay key secret, SMTP password and admin PIN were readable in DevTools, in any HAR capture, and in the Level-2 support export — an admin-gated route, but every one of those credentials sat in the page of anyone who ever opened Settings. Added `SECRET_SETTINGS_KEYS` + `redactSettings()` / `unredactSettings()` to `backend/defaultSettings.js`, beside the template that defines the keys, so a future secret is declared once and every emission point inherits it. Wired into all three leak points: `GET /api/settings`, the `POST /api/settings` echo, and the `GET /api/diagnostics/export` bundle.<br>- **The round-trip was the hard part.** SettingsManager is a read-modify-write form: it renders what GET returned and posts the fields back, so naive redaction would have written a row of bullets over the tenant's real SMTP password the next time anyone saved the company address. `unredactSettings()` restores any value that comes back still equal to the mask; a genuinely retyped secret no longer matches and saves normally. An unset credential stays an empty string rather than becoming a mask, so the UI can still distinguish "not configured" from "hidden". Three `text-muted-small` hints added to `SettingsManager.js` so the mask reads as intentional.<br>- **`npm audit` is clean (was 1 high + 2 moderate).** nodemailer `^6.9.13` → `^9.0.5` (8 advisories incl. SMTP command injection) and node-cron `^3.0.3` → `^4.6.0` (transitive `uuid` bounds check). Both are major bumps, so compatibility was probed empirically in a scratch install against this codebase's actual call shapes — `createTransport({host,port,secure,auth})`, `sendMail({from:"Name" <addr>,to,subject,html})`, the "unreachable host must reject catchably" contract `sendMailIfConfigured()` depends on, and all five real `cron.schedule()` expressions including that a task still auto-starts without an explicit `.start()`. All green; **no source changes were required**. Dependency budget unchanged at 6.<br>- **First HTTP route tests.** The 6 existing integration blocks were helper-level — nothing exercised a route, an auth boundary, or a persistence failure. New `backend/test_routes.js` boots the real server as a child process on a free ephemeral port against a throwaway data dir and drives it over `fetch`: 27 checks across the public surface, the admin auth boundary (missing/garbage/empty bearer, wrong PIN, license-gate-open 401s), redaction, the masked round-trip, a persistence failure injected by making `writeJSON`'s staging path a directory (EISDIR), the invoice-sequence 409 guard, session invalidation on logout, and the brute-force 429 lockout.<br>- **Enabling change:** `DATA_DIR`/`LOGS_DIR` in `db.js` now honour `GOLDPOS_DATA_DIR`/`GOLDPOS_LOGS_DIR`, defaulting to the in-tree directories exactly as before. Nothing shipped sets them; they exist so the route suite never writes into a live `backend/data/` (CLAUDE.md §8) and can run alongside a dev server on :5000.<br>- **Verified:** 57 billing + 6 integration + 27 route checks green. Both halves of the redaction fix were proven by temporarily neutering `redactSettings()` and then `unredactSettings()` and confirming the suite went red each time (`razorpayKeySecret not masked`, then `razorpayKeySecret was overwritten by the mask!`) rather than trusting a green run. `npm audit` → 0 vulnerabilities.<br>- Built on branch `phase-21-hardening` in an isolated worktree because a concurrent session was actively editing `server.js`/`db.js`/`billingMath.js` in the main tree at the time. | - [backend/defaultSettings.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/defaultSettings.js)<br>- [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/db.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/db.js)<br>- [backend/test_routes.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_routes.js)<br>- [backend/package.json](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/package.json)<br>- [frontend/js/components/SettingsManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/SettingsManager.js)<br>- [CHANGELOG.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CHANGELOG.md)<br>- [docs/TESTING_CHECKLIST.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/TESTING_CHECKLIST.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md) |


| 2026-08-08 | -- | AI Assistant | **Phase 20.2: Closing the three gaps left open by 20.1 — no admin screen, unbound payment amount, instant credit for unverified UPI**<br>- **Manual UPI no longer credits on the customer's word.** `POST /api/customer/advances` wrote a deposit that counted immediately, so a signed-in customer could type any amount and a made-up reference and redeem the balance against a real bill. Deposit rows now carry `status`; a portal-submitted UPI claim is written `pending` and holds no balance until a cashier approves it at `POST /api/advances/:id/approve`. Counter entries and signature-verified Razorpay stay `approved` on arrival — a cashier or a valid HMAC already supplies the confirmation the review step exists to get.<br>- **Backward compatibility is the load-bearing part:** a *missing* `status` reads as approved, because every advance row already on a tenant's disk predates the field and is money the store genuinely took. Defaulting the other way would have zeroed every existing customer's balance on upgrade. Verified against the real `backend/data/advances.json` (3 status-less rows): balances stayed 11,000 and 500.<br>- **One choke point for the balance, not three.** `computeAdvanceLedger` (server), the Dashboard's outstanding-advances tile, and `AdvancesManager`'s per-customer rollup each summed `advances.json` by hand, so a pending row would have counted as real credit in whichever was missed. All three now route through new `advanceEntryDelta` / `computeAdvanceBalance` / `summarizeAdvanceLedger` helpers in the shared `frontend/js/lib/billingMath.js`, alongside the invoice math.<br>- **Reference IDs are now unique** across the ledger, enforced inside `recordAdvanceDeposit` so every present and future caller inherits it — case- and whitespace-insensitive. Without it the same UTR could be submitted three times and each row would look individually plausible to whoever approved it.<br>- **`POST /api/payment/verify` no longer credits `req.body.amount`.** Razorpay's HMAC covers `order_id\|payment_id` only — there is no amount in the signed text — so a customer could create a ₹100 order, pay it, and post back `amount: 500000`. Orders are now persisted to `backend/data/payment_orders.json` at creation with the session's phone and the amount, and verification reads the amount from that record, ignoring the body entirely. Also binds the order to its owner (a second customer verifying someone else's order id → 403) and rejects unknown order ids → 400. Pruned on write (24h TTL, 2,000-row cap) since an order is a short-lived intent, not history.<br>- **The replay short-circuit was deliberately moved ahead of the order lookup**, so a late retry of a payment that *was* credited still returns its ledger row rather than the "order not recognised" error a pruned lookup would produce.<br>- **Item 1: the admin screens that did not exist.** `GET /api/customer-accounts` and `POST /api/customer-accounts/issue-login` had been reachable only by hand-crafted HTTP since 20.1 — `grep -rln "customer-accounts" frontend/` found nothing. New "Customer Logins" tab (`CustomerAccountsManager.js`): lists logins with lockout/temp-password/active state, issues and reissues them, and shows the one-time temporary password *in the page* rather than through `alert()`, since a dismissed alert can only be recovered by another reset. The pending-deposit approval queue went into the Advances tab instead, where the ledger already lives.<br>- **Every surface that displays a deposit now distinguishes claimed from received** — the approval queue, the ledger drill-down, the dashboard's recent-deposits list, and the customer's own history and Gold Appreciation panel (a pending deposit contributes no grams, so that panel and the balance above it describe the same money). The portal shows an explicit "awaiting the store's confirmation" note, because an unchanged balance after submitting otherwise reads as lost money.<br>- **All advances writes go through `writeJSONTransaction`**, the journalled path added in `5bfc950`, rather than a bare `writeJSON` — `/api/sales` already commits `advances.json` inside a transaction, and two write mechanisms on one ledger is the parallel path §1 forbids. **Open item:** `writeJSONTransaction` does a bare `renameSync` without the EPERM/EBUSY retry loop `writeJSON` carries for Windows lock contention (§9); it should inherit it.<br>- **Verification:** 71 billing checks green (14 new, covering the missing-status default, pending/rejected exclusion, the unapproved-claim-cannot-be-redeemed path end to end, case-insensitive status matching, negative/non-numeric amounts, and paise settlement); `node --check` on all 7 touched JS files plus the extracted `customer.html` module; live end-to-end run on the real data directory (backed up first, restored and diffed afterwards) covering legacy balance preservation, issue-login → forced password change → pending deposit → balance unmoved at 11,000 → approve → 61,000, duplicate reference → 409 including a case/space variant, double-approve → 409, reject-without-reason → 400, reject leaving the balance untouched, the ₹500,000 forgery credited as ₹100, cross-customer order theft → 403, unknown order → 400, and replay → `duplicate: true` with no second credit. | - [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/db.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/db.js)<br>- [backend/test_billing_math.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_billing_math.js)<br>- [frontend/js/lib/billingMath.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/lib/billingMath.js)<br>- [frontend/js/components/CustomerAccountsManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/CustomerAccountsManager.js)<br>- [frontend/js/components/AdvancesManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/AdvancesManager.js)<br>- [frontend/js/components/Dashboard.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/Dashboard.js)<br>- [frontend/js/app.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/app.js)<br>- [frontend/index.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/index.html)<br>- [frontend/customer.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/customer.html)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md)<br>- [docs/TESTING_CHECKLIST.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/TESTING_CHECKLIST.md) |

| 2026-08-09 | -- | AI Assistant | **Production-readiness Phase 0 remediation: gateway truth, server-authoritative pricing, fail-closed boot, seeded fixtures, E2E journeys** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 0 — 7 items closed, 2 corrected rather than ticked)<br>- **Nothing ever asked Razorpay whether the money arrived.** `/api/payment/verify` treated a valid checkout signature as proof of payment, but Razorpay's HMAC covers `order_id\|payment_id` and nothing else — an authorised-but-uncaptured payment, one that later failed, one already refunded, and one captured for a different amount all produce a signature that verifies perfectly. Verification now calls `GET /v1/payments/:id` and credits only on `status: 'captured'` for the order's exact paise; `authorized` returns 202 and defers to the webhook; a failed gateway lookup returns 503 without crediting, because the webhook is the safety net for exactly that case.<br>- **New `POST /api/payment/webhook`** — the authoritative channel, since the browser callback is a courtesy the customer can close a tab on. Mounted with `express.raw()` **before** the global `express.json()` so the HMAC covers the exact bytes sent (a parsed-then-reserialised body cannot be verified); idempotent on the gateway's own event id in `payment_events.json`; tolerant of out-of-order delivery; exempted from the licence gate in `licenseChecker.js`, because refusing a callback does not un-take the customer's money, it only loses the record of it. Returns 2xx for anything it consciously declines to act on so Razorpay stops retrying, and 5xx (releasing the event-id claim) only when a genuine apply failed.<br>- **One credit path, not two.** `creditCapturedPayment()` is the single routine both the checkout and webhook paths end in; the checkout/webhook race is made safe rather than merely unlikely by the existing `referenceId` uniqueness constraint inside `recordAdvanceDeposit` — whichever arrives second is rejected as a duplicate and answered with the original row.<br>- **`/api/sales` recomputed tax and discount over a metal value the client chose,** which made the whole server-side recomputation decorative: correct percentages of a fabricated base. The rate now comes from `getActiveGoldRates()` keyed by purity and `metalValue` from weight × that rate, with `goldRateSource` recorded so an audit can tell a counter override from a synced market rate. A drifted client rate is reported back as `rateCorrected` (mirroring the existing `totalCorrected`) so the desk reprints rather than letting the slip and the ledger disagree. A sale is refused 503 rather than priced against a zero rate.<br>- **`...req.body` is gone from the persisted sale.** Spreading the body put any key the client sent into the permanent ledger — including a client-chosen `timestamp`, which decided the annual partition and tax period an invoice was filed under. Replaced with an explicit allowlist; the timestamp is the server's clock.<br>- **`backend/productionGuard.js` — the process now refuses to boot** under `NODE_ENV=production` with demo Razorpay keys, no webhook secret, no https public URL, the default `1234` PIN, the `mock` price provider, or a `NODE_ENV`/`ENV_NAME` disagreement. The demo-credential checks it replaces were per-request, so a production install would serve happily all day and only fail at the moment a real customer tried to pay. Reports every blocker at once. Inert outside production by design — local dev and every suite use demo keys deliberately.<br>- **Cryptographically strong ledger ids.** `newId()` in `db.js` (`crypto.randomBytes`) replaces `'ADV-' + Math.random().toString(36)`: seven base-36 characters from a non-cryptographic PRNG is ~78 billion values, which by the birthday bound collides in the low hundreds of thousands of rows, and is guessable rather than merely unlikely. One generator for every write path.<br>- **Payment orders in paise.** Rows carry `amountPaise` (integer, authoritative), `currency`, `status` and `expiresAt`; `amount` stays as a rupee mirror so rows written by earlier builds keep reading. `toPaise`/`fromPaise`/`computeMetalValue` went into `frontend/js/lib/billingMath.js` with the rest of the money math rather than living in `server.js` — the payment routes are a caller, not an owner.<br>- **`backend/seed.js` (`npm run seed`) — reproducible synthetic fixtures.** Seeded mulberry32 PRNG and a fixed epoch, so two runs are byte-identical (verified by diff); invented names, the `90000000xx` phone block and `@example.test` addresses, so nothing can dial or mail a real person if a fixture leaks; refuses to write into `backend/data/` without `--force`. Covers approved/pending/rejected/redeemed advances and two calendar years of invoices. Password hashes use a derived-but-fixed salt and are re-verified through the real `verifyPassword()` before the file is written, so a change to the hash format fails the seeder loudly instead of producing accounts nobody can log into.<br>- **`backend/tests/e2e/` — Playwright cashier and customer journeys**, each booting its own server on an ephemeral port against its own seeded temp database. **Finding along the way:** both `index.html` and `customer.html` override `window.alert` with an in-page `#custom-alert-box`, so `page.on('dialog')` never fires and the undismissed overlay silently swallows every subsequent click — the specs assert through a shared `readAlert()` helper instead. A second gotcha is documented in `openBillingDesk()`: `BillingDesk.init()` wires its listeners only after awaiting `/api/gold-price` and `/api/settings`, so filling the form early types into a live-looking form whose Save button does nothing.<br>- **New settings keys** `razorpayWebhookSecret` (redacted, write-only, in `SECRET_SETTINGS_KEYS`) and `publicUrl`, reaching existing tenants through the established `getDefaultSettings()` merge. Both have Settings UI, including the exact webhook URL to paste into the Razorpay dashboard.<br>- **CI gates fixed as part of the E2E item:** all four workflows ran `npm install` and only `test_suite.js`, so four of the five suites — including every money-path test — would never have executed in CI. Now `npm ci` + `npm test`.<br>- **Verified:** `cd backend && npm test` → 83 billing + 6 integration + 27 route + 25 HTTP + 16 guard = **157 checks green**. `npm run test:e2e` → **16/16 green** (Desktop Chrome + 390px Pixel 7). The guard suite's last two checks boot a real server process with demo settings and assert exit code 1, rather than only unit-testing the predicate. `backend/data/` untouched throughout — every suite uses a temp directory. One hand-worked expectation in the new paise tests was wrong (₹1.005 is not representable and rounds to 100 paise, not 101); it was corrected to pin the `toPaise`/`round2` agreement instead of the idealised decimal. | - [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/db.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/db.js)<br>- [backend/defaultSettings.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/defaultSettings.js)<br>- [backend/licenseChecker.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/licenseChecker.js)<br>- [backend/productionGuard.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/productionGuard.js)<br>- [backend/seed.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/seed.js)<br>- [backend/playwright.config.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/playwright.config.js)<br>- [backend/tests/e2e/fixtures.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/fixtures.js)<br>- [backend/tests/e2e/cashier-billing.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/cashier-billing.spec.js)<br>- [backend/tests/e2e/customer-portal.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/customer-portal.spec.js)<br>- [backend/test_production_guard.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_production_guard.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js)<br>- [backend/test_billing_math.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_billing_math.js)<br>- [backend/package.json](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/package.json)<br>- [frontend/js/lib/billingMath.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/lib/billingMath.js)<br>- [frontend/js/components/BillingDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/BillingDesk.js)<br>- [frontend/js/components/SettingsManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/SettingsManager.js)<br>- [.github/workflows/cd-dev.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-dev.yml)<br>- [.github/workflows/cd-sandbox.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-sandbox.yml)<br>- [.github/workflows/cd-live.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-live.yml)<br>- [.github/workflows/daily-checks.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/daily-checks.yml)<br>- [.gitignore](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.gitignore)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/PRODUCTION_READINESS_ROADMAP.md)<br>- [docs/ai_handover.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/ai_handover.md)<br>- [docs/TESTING_CHECKLIST.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/TESTING_CHECKLIST.md)<br>- [CHANGELOG.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CHANGELOG.md)<br>- [CLAUDE.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CLAUDE.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md) |
| 2026-08-09 | -- | AI Assistant | **Phase 22: self-service password reset, 10-digit customer number enforcement, tax-base proof, and the Reprint Desk**<br>- **Customer password reset needed the store for every case that mattered.** Three separate gates made it so: `customer.html`'s "Forgot password?" fired an `alert()` and refused to open the pane at all when the tenant had no SMTP configured, leaving the customer back on the sign-in screen with nothing to act on; self-registration treated email as optional, so a customer could create an account with no address and therefore no way ever to reset it; and counter-issued logins routinely carry no email, which makes `POST /api/customer/password/forgot` a silent no-op forever. Fixed at all three: the pane now always opens and states the SMTP situation inline beside a disabled send button, registration requires a valid email (client-side — the server still allows email-less accounts because counter issuance legitimately creates them), and the portal's landing tab carries a prompt that takes a customer with no address straight to the Account field and clears itself on save. `POST /api/customer-accounts/issue-login` now returns `hasEmail` so the counter handover screen can tell the cashier to have the customer add one. Settings' SMTP block states, in place, whether customer self-service reset is currently live — SMTP reads as a *reporting* setting, so a store that left it blank had no way to know it had also switched off every customer's reset.<br>- **The reset-code machinery had no test coverage at all**, despite `test_suite.js` Test 5's comment claiming it was "proved against a live server" (no suite touched it). Added Test 7: single use, expiry, the 5-guess budget burning the real code, weak-password refusal on a valid code, no code for an unknown number, and the plaintext code never appearing in `customer_auth.json`. Found and fixed en route that `test_suite.js` resolved `DATA_DIR` from the real `backend/data/` — ESM caches `db.js` at first import, so redirecting the env var inside a test function is too late. The whole suite now runs against a throwaway temp tree (CLAUDE.md §8).<br>- **The Billing Desk let a partial customer number through.** `POST /api/sales` has always rejected anything but exactly 10 digits, but the desk posted it anyway and surfaced the 400 as a generic "Failed to save invoice" naming no field. `submitSale()` now validates first, reading the value off the input rather than off `this.customerPhone` (which only the `input` event refreshes, so autofill and OS-level paste bypassed it), and reports on the field itself. Blank still files as a cash sale.<br>- **Tax inclusive/exclusive on the whole bill including making charge: verified correct, not changed.** `computeInvoiceTotals()` already took `preTaxTotal = metal + making` as the taxable base in both modes, and both the server and the browser route through it. Added `test_billing_math.js` group 11 (8 checks) to make the property explicit and isolated — each check moves only the making charge and asserts the tax moves with it, so a regression to a metal-only base fails rather than passing silently. Covers a making-charge-only bill (labour/repair jobs, which a metal-driven base would zero-rate) and the discount applying to the combined base. Invoice line relabelled `Taxable Value (Metal + Making)` on both the live and reprint sheets — this is the line customers query at the counter.<br>- **New Reprint Desk** (`frontend/js/components/ReprintDesk.js`, `GET /api/sales/lookup`): find a filed invoice by number, phone, name, or date range and print a second copy stamped `DUPLICATE — REPRINT`. Every figure is the stored record — never re-priced against today's gold rate or tax settings, which is asserted by an E2E test that moves the rate to ₹9,999/g and the slab to 18% Inclusive between filing and reprinting. Pre-Phase-20 records that predate `taxableAmount`/`taxAmount`/`taxMode` print their filed total with the tax lines marked *not recorded* rather than as ₹0.00, since a zero GST line is a claim about a tax period this system never recorded.<br>- **`PRINT INVOICE` printed a blank page and had for some time.** The print stylesheet hid `.tab-panel:not(#tab-billing)` — an id present nowhere in the markup (the panel is `#sales-tab`), so under print media it matched the billing panel too and hid the sheet it was meant to print. `.billing-container` and `.billing-form-col` were equally dead (`.billing-desk-grid`, `.billing-inputs-card`). Rewritten to key off `.tab-panel.active`, which fixes billing, covers reprint, and needs no change when a tab is added. Guarded by two E2E tests using `emulateMedia({ media: 'print' })` — the only way to catch this, as it is invisible on screen.<br>- Tests: `npm test` 5/5 suites green (91 billing-arithmetic checks, integration incl. new Test 7, 27 route/auth, 25 HTTP, 16 production-guard). E2E 30/30 (21 desktop + 9 mobile), including a new `reprint-desk.spec.js` (6) and new cashier/portal cases for the phone guard and the reset paths. | - [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/test_suite.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_suite.js)<br>- [backend/test_billing_math.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_billing_math.js)<br>- [backend/tests/e2e/reprint-desk.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/reprint-desk.spec.js)<br>- [backend/tests/e2e/cashier-billing.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/cashier-billing.spec.js)<br>- [backend/tests/e2e/customer-portal.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/customer-portal.spec.js)<br>- [frontend/js/components/ReprintDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/ReprintDesk.js)<br>- [frontend/js/components/BillingDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/BillingDesk.js)<br>- [frontend/js/components/SettingsManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/SettingsManager.js)<br>- [frontend/js/components/CustomerAccountsManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/CustomerAccountsManager.js)<br>- [frontend/js/app.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/app.js)<br>- [frontend/index.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/index.html)<br>- [frontend/customer.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/customer.html)<br>- [frontend/css/app.css](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/css/app.css) |
| 2026-08-11 | -- | AI Assistant | **Phase 23: Returns & Refunds — the store gives money back, in cash or in gold**<br>- **New Return Desk** (`frontend/js/components/ReturnDesk.js`, nav tab after Reprint Invoice) and a new year-partitioned ledger `backend/data/returns_YYYY.json`, filed under the year the **refund** happened rather than the year of the invoice it reverses — a January refund of a December sale belongs in January's books.<br>- **The refund is priced by the original invoice, never by today.** `computeReturnRefund()` in `frontend/js/lib/billingMath.js` rebuilds the refund from the stored sale record's own rate, making charge, discount, slab and tax mode, through the same `computeInvoiceTotals()` pipeline that priced the sale. Re-pricing against the current rate would over-refund after a rise and short-change the customer after a fall.<br>- **What is refunded is `totalAmount + appliedAdvance`, not `totalAmount`.** An advance redeemed on the original bill was the customer's own money being spent, so the value owed back includes it; re-crediting the advance separately would pay the same rupees out twice.<br>- **Partial returns by weight, tracked cumulatively.** A 13.435g invoice can come back 6g today and 7.435g next week. The closing return is trued up to the exact unrefunded remainder and every refund is capped by it, so however an invoice is split up the refunds sum to its filed gross to the paise. Return state is **derived** from the returns ledger on every read (`summarizeInvoiceReturns` / `withReturnState`) — the sale record is never rewritten, so a reprint still reproduces the original document.<br>- **Two refund modes.** `cash` writes only the return row. `gold` additionally credits the customer's advance ledger as an approved deposit with a locked 22K rate, committed in the **same** `writeJSONTransaction` as the return — a crash between the two would otherwise leave either a refund nobody was credited for or credit against a return that never happened. Extracted `buildAdvanceDepositRow()` so the refund credit and a counter deposit are the same row shape rather than two subtly different ideas of one.<br>- **Store-only, by design.** `POST /api/returns` and `GET /api/returns` are admin-gated; there is deliberately no customer-facing way to raise, request or cancel a return. Customers **see** theirs through the new session-scoped `GET /api/customer/returns` (cashier notes and the phone are excluded from that view).<br>- **Reflected on mobile.** `customer.html`'s history tab now merges cash refunds as their own rows and relabels gold refunds as `RETURN CREDIT` against the invoice they came from. Gold refunds appear once, not twice: the credit row is the one that moved the balance, so listing the return beside it would show the same rupees twice and make the history disagree with the balance.<br>- **Invoices whose breakdown was never stored still refund correctly.** A pre-Phase-20 record (no `taxableAmount`/`taxAmount`), or one whose stored figures do not reconcile, falls back to a straight pro-rata share of the filed gross and the credit note says the breakdown is unavailable rather than inventing a GST line.<br>- **Books stay consistent:** the daily/monthly email report now nets refunds off revenue (showing gross, returns and net when any exist), and the Level-2 diagnostics export bundles `returns_*.json` alongside `sales_*.json`.<br>- **Verified:** `npm test` → **5/5 green, 216 checks** (114 billing incl. 22 new return checks, integration, 27 route, 44 HTTP incl. 19 new return checks, 16 guard); `npm run test:e2e` → **43/43** (31 desktop + 12 mobile) including new `return-desk.spec.js` (7 journeys) and three customer-portal return journeys asserted at 390px. `npm run seed` now ships 3 returns (2 cash, 1 gold credit). | - [frontend/js/lib/billingMath.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/lib/billingMath.js)<br>- [frontend/js/components/ReturnDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/ReturnDesk.js)<br>- [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/emailReporter.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/emailReporter.js)<br>- [backend/seed.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/seed.js)<br>- [frontend/customer.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/customer.html)<br>- [frontend/index.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/index.html)<br>- [frontend/js/app.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/app.js)<br>- [backend/test_billing_math.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_billing_math.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js)<br>- [backend/tests/e2e/return-desk.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/return-desk.spec.js)<br>- [backend/tests/e2e/customer-portal.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/customer-portal.spec.js) |
| 2026-08-11 | -- | AI Assistant | **Phase 24: ADR-001 accepted, the transactional schema, and the CI gate that had never run** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 1 — foundation laid; routes not yet cut over)<br>- **Two owner decisions were taken and recorded, unblocking Phase 1.** (1) **ADR-001 → SQLite** via the stdlib `node:sqlite`, behind repository interfaces, with PostgreSQL kept as the documented destination and four explicit revisit triggers written into the ADR. The roadmap had recommended Postgres; the argument that flipped it is that the deployment is already one process per tenant with its own data directory, so SQLite maps 1:1 onto it, adds **zero** dependencies (it is in the standard library, so CLAUDE.md §0's budget is untouched), keeps `backupEngine.js`'s file-copy strategy working, and needs no server on a VPS that does not exist yet. The repository seam that makes it reversible is Phase 1 item 4 *regardless* of engine, so choosing the bridge first costs almost nothing. (2) The manual-UPI approver is a **distinct named role**, with the minimal identity slice pulled forward into Phase 1 — a PIN would have been a second auth mechanism beside the admin PIN (§1 forbids the parallel path) and would answer "who approved this claim?" with "someone who knew the PIN".<br>- **Owner decision 6 answered: there is no SKU concept.** Roadmap §4's entire Catalogue domain — products, variants, lots, barcodes, stock movements/counts/transfers — is struck, as is the "stock" leg of the one-transaction item. That was the most relational and most write-concurrent part of the target schema, and removing it is a large part of why the SQLite bridge is sufficient.<br>- **`backend/repositories/` — the seam.** `connection.js` opens the single handle with WAL, `foreign_keys=ON` (SQLite defaults it OFF *per connection*, so every referential guarantee in the schema would otherwise be inert), `busy_timeout=5000`, `synchronous=FULL` (a counter PC gets its plug pulled; NORMAL can lose the last transaction) and `trusted_schema=OFF`. `inTransaction()` nests via SAVEPOINT so a service composed of two smaller services still commits as **one** unit — without it, "one ACID transaction per sale" quietly becomes four.<br>- **`001_initial_schema.sql` — 19 tables.** Every quantity is a scaled INTEGER with the scale in the column name (`_paise`, `_mg`, `_paise_per_g`, `_bp`); there is not one REAL in the schema, which is the point of the phase. `tenant_id`/`branch_id` are on every tenant-owned row even though today is one database per tenant — they cost nothing now and are the difference between a migration and a rewrite when ADR-001's revisit trigger fires.<br>- **The constraints the roadmap asked for are now constraints, not conventions:** unique invoice number *and* unique financial-year sequence slot; partial unique indexes on idempotency keys (so many rows may omit one but no two may share one); unique provider order id, provider payment id and webhook event id — the last is what makes a gateway retry structurally unable to credit twice, as a constraint rather than a lookup-then-insert with a race in the middle.<br>- **Advances are genuinely append-only, enforced by trigger.** Financial fields are frozen at insert (`amount_paise`, `entry_type`, `account_id`, `reference_id`, …); DELETE is refused outright; status transitions are restricted to `pending→posted|rejected` and `posted→reversed`, so a rejected claim can never be flipped to posted. Amounts are **signed** (deposit > 0, redeem < 0) so a balance is a plain `SUM` no future caller can get the sign wrong on. A `CHECK (status <> 'posted' OR approved_by_user_id IS NOT NULL)` is the manual-UPI reconciliation control expressed where it cannot be bypassed, and an `approvers` view exposes only active owners and managers. `audit_events` is immutable the same way.<br>- **`migrate.js`** — idempotent (the server can call it on boot, so a tenant upgrades by restarting), transactional per migration (SQLite makes DDL transactional, so a failure leaves the schema untouched), and **tamper-evident**: each applied migration's SHA-256 is recorded and editing an already-applied file is refused by name, because the file no longer describes what is on disk. An older build meeting a newer database logs rather than refusing to boot — a rollback should not become an outage.<br>- **The CI gate had never executed, and the cause was not the missing deploy target.** `daily-checks.yml` — the one gate needing no infrastructure — triggered only on push-to-`main` and a nightly cron, while all work happens on feature branches and `origin/main`, `origin/develop` and `origin/staging` are all still at `e4999bc`. The cron had therefore only ever re-tested a nine-commit-old `main`. Added a `pull_request` trigger on every branch plus pushes to the three pipeline branches, and renamed it "CI — tests & security audit" to match what it actually is. Still unticked in the roadmap until a run is *observed* green on GitHub.<br>- **Node floor 20 → 24** (`node:sqlite` is only stable and flag-free from 24) across `backend/package.json`, all four workflows and `deploy/README.md`.<br>- **Verified:** `cd backend && npm test` → **6/6 suites, 259 checks green, exit 0** (114 billing + **43 new schema** + integration + 27 route + 44 HTTP + 16 guard). The new `test_schema.js` asserts every invariant by *attempting the violation and requiring a throw* — a schema is not evidence, a rejected INSERT is. Two of its own helpers were caught silently dropping overrides whose columns were missing from a hand-written INSERT list (the test would have passed for the wrong reason); both now derive their column list from the data. `backend/data/` untouched — the suite sets `GOLD_POS_DATA_DIR` on its first line, per §8. | - [docs/adr/ADR-001-transactional-datastore.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/adr/ADR-001-transactional-datastore.md)<br>- [backend/repositories/connection.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/connection.js)<br>- [backend/repositories/migrate.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/migrate.js)<br>- [backend/repositories/migrations/001_initial_schema.sql](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/migrations/001_initial_schema.sql)<br>- [backend/test_schema.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_schema.js)<br>- [backend/package.json](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/package.json)<br>- [.github/workflows/daily-checks.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/daily-checks.yml)<br>- [.github/workflows/cd-dev.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-dev.yml)<br>- [.github/workflows/cd-sandbox.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-sandbox.yml)<br>- [.github/workflows/cd-live.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-live.yml)<br>- [deploy/README.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/deploy/README.md)<br>- [CLAUDE.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CLAUDE.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/PRODUCTION_READINESS_ROADMAP.md)<br>- [docs/ai_handover.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/ai_handover.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md) |
| 2026-08-12 | -- | AI Assistant | **Phase 25: the four audit gaps — actor identity, multi-line invoices, tenders, and bounded ledger reads**<br>- **WHO. Every financial record now names a person.** One anonymous PIN used to gate the whole desk, so a sale, a discount, a counter rate override and a cash refund were all filed with nobody attached. Named operators with per-person PINs and the four schema roles (`owner`/`manager`/`cashier`/`auditor`) live in Settings → **Staff & Roles**; the PIN both authenticates and identifies (`resolveActor` in `adminAuth.js`). `requireAdminSession` attaches `req.actor` at the one choke point every admin route already passes through, and the actor is stamped on the sale, the return/refund, the advance redemption, the counter deposit and the deposit approval. New `requireApprover` gates deposit approve/reject to owner/manager and the row records `reviewedBy` — closing the Phase 0 manual-UPI "manager reconciliation" item, which could not close while "manager" did not exist as a concept. Operator PINs are write-only to the browser (`redactSettingsForBrowser` + `mergeOperators` restoring by **id, not index**, so a save that reorders the roster cannot move one cashier’s PIN onto another) and masked in the support export via a new `operators.*.pin` wildcard in `SECRET_SETTINGS_KEYS`. Backward compatible: a store with no operators configured still signs in on the master PIN, resolving to the `owner` bootstrap identity.<br>- **AN INVOICE IS NOW A LIST OF ITEMS.** `POST /api/sales` accepts `lines[]` (up to 50), each with its own purity, weight, store-side rate, making charge and discount; `computeInvoiceTotals` takes `lines` and returns a per-line allocation. The header figures are computed exactly as before over the summed lines and then **split across the lines in integer paise by largest remainder**, so `sum(line.taxableAmount) === invoice.taxableAmount`, `sum(line.taxAmount) === invoice.taxAmount` and `sum(line.lineTotal) === invoice.totalBeforeAdvance` hold exactly at every slab in both tax modes. A one-line invoice prices to the identical paise as before — asserted by the 114 pre-existing billing checks, all still passing untouched. The stored record carries `lines[]` **and** the old flat rollup (with `purity: 'MIXED'` / `goldPricePerGram: 0` where no single value exists), read through the new `saleLines()` seam, so every invoice already on disk stays reprintable and returnable.<br>- **A SALE NOW RECORDS HOW IT WAS PAID.** Not "no split tender" — there was previously no payment method on a sale at all, so a drawer count, a shift variance and a settlement reconciliation were not features waiting to be built, they were arithmetic with no data to perform it on. `tenders[]` (≤10, methods matching `tenders.method` in the SQL schema) is validated in integer paise to sum **exactly** to the amount payable after any advance redemption, and is refused *before* an invoice number is consumed. A lone tender sent with no amount means "the whole bill, this way", which is what keeps a legitimately repriced invoice (overnight rate sync, slab edited mid-shift) from being refused for disagreeing with the browser’s stale total.<br>- **RETURNS ARE PER LINE.** A return names its line, because a cart holding 22K bangles and an 18K chain has two rates and two making charges on it, and "5 g came back" is not answerable until the line is known. Each line’s returnable weight is its own, so exhausting line 2 no longer consumes line 1’s; the closing return is still trued up to the invoice’s unrefunded remainder, so all refunds against one invoice still sum to its filed gross to the paise however it was split up.<br>- **THREE UNBOUNDED LEDGER READS CLOSED.** `GET /api/sales`, `/api/returns` and `/api/advances` each returned the entire ledger as a bare array, and the Dashboard, Return Desk and Advances tab each downloaded a store’s whole trading history on tab open. All three now answer `{results, total, truncated, limit}` behind a shared `parseLedgerQuery`/`pagedLedger` — the pattern `/api/sales/lookup` already had, lifted into one place and now reused by it too. **The aggregates those screens were summing moved to the server**, computed over the whole matched set, because otherwise paging would have silently understated a revenue tile the day the ledger outgrew one page. New `GET /api/advances/customers` rolls balances up per customer server-side, since a spendable balance is a customer’s whole history and cannot be computed from a page; the per-customer drill-down now fetches one customer’s rows on demand.<br>- **Doc drift corrected.** Roadmap Phase 5 slices 3 and 4 read as untouched though Phase 22 shipped reprint and Phase 23 shipped returns/refunds/credit-notes; both now record what shipped when, plus a cost note on why "multi-line sale" was the largest hidden item in that phase. `CLAUDE.md` §2 listed **wastage** as money math requiring test coverage — wastage exists nowhere in the tree (no field, helper, setting or test); removed from the rule and recorded in the roadmap as an unscoped catalogue attribute needing a product decision on whether it is a weight uplift, a share of making charge, or a charged line.<br>- **Deleted:** dead `unredactSettings()` (nothing imported it — the live write path masks with `null` + a `*Configured` flag and restores through `preserveWriteOnlyValue`, so a second unredaction mechanism was only a way for the two to disagree) and dead `Dashboard.renderPurityMix()` (its call site was already commented out).<br>- **Tests: 259 → 309 checks.** `test_billing_math.js` 114 → 140 (multi-line allocation identities across every slab and both tax modes, per-line returns, `saleLines()` on both record shapes, the advance-liability rollup); `test_http.js` 44 → 68 (multi-line sales, tender validation, actor identity, the approver gate, paged ledgers). All 43 Playwright journeys still pass; their Billing Desk readiness signal moved to an explicit `#sales-tab[data-desk-ready="true"]` because the invoice preview is a per-line table now and renders nothing until an item is entered. **Verified end-to-end against a running server on a scratch tenant**, not only by tests: a two-line 22K+18K invoice (₹1,17,420) split across cash and card, its rows summing exactly to the header; a per-line return priced at that line’s own rate; a second return against the same line refused while the other line stayed returnable; a cashier refused an approval by name and a manager granted it; operator PINs absent from the settings response. | - [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/adminAuth.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/adminAuth.js)<br>- [backend/defaultSettings.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/defaultSettings.js)<br>- [frontend/js/lib/billingMath.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/lib/billingMath.js)<br>- [frontend/js/app.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/app.js)<br>- [frontend/index.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/index.html)<br>- [frontend/js/components/BillingDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/BillingDesk.js)<br>- [frontend/js/components/ReturnDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/ReturnDesk.js)<br>- [frontend/js/components/ReprintDesk.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/ReprintDesk.js)<br>- [frontend/js/components/Dashboard.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/Dashboard.js)<br>- [frontend/js/components/AdvancesManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/AdvancesManager.js)<br>- [frontend/js/components/SettingsManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/SettingsManager.js)<br>- [backend/test_billing_math.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_billing_math.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js)<br>- [backend/tests/e2e/cashier-billing.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/cashier-billing.spec.js)<br>- [backend/tests/e2e/reprint-desk.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/reprint-desk.spec.js)<br>- [backend/tests/e2e/return-desk.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/return-desk.spec.js)<br>- [CLAUDE.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CLAUDE.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/PRODUCTION_READINESS_ROADMAP.md)<br>- [docs/TESTING_CHECKLIST.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/TESTING_CHECKLIST.md)<br>- [docs/ai_handover.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/ai_handover.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md) |
| 2026-08-13 | -- | AI Assistant | **Phase 26: the repository seam, the domain services, the JSON importer, and five bugs the tests found** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 1 — everything below the routes; the route cut-over is deliberately NOT in this entry)<br>- **The seam.** Ten repositories under `backend/repositories/`, one per domain (organisation, users, customers, rates, sequences, invoices, credit notes, advances, payments, audit), fronted by `repositories/index.js`. No SQL string exists above that directory, which is the condition ADR-001 §3 attached to choosing SQLite — it is what keeps the documented move to PostgreSQL a swap rather than a rewrite.<br>- **The wire shape did not change.** `toLegacySale()`, `toLegacyReturn()` and `toLegacyAdvance()` project rows back into the exact shapes `sales_YYYY.json`, `returns_YYYY.json` and `advances.json` held, so every screen, the customer portal, the diagnostics export and both HTTP suites keep working untouched. `test_repositories.js` asserts the full key set of each shape, so a dropped field fails as loudly as a renamed one.<br>- **Four domain services** (`backend/services/{sale,return,advance,payment}Service.js`). A sale is now ONE transaction: number allocation, header, lines, tenders, advance redemption and the audit row commit together or not at all. The advance-balance check runs inside that transaction, which is the "reserve funds safely during checkout" item — two tills redeeming one balance serialise on the write lock instead of both reading the same figure.<br>- **The importer** (`backend/importLegacyJson.js`, `npm run import:legacy[:dry-run]`). Dry run writes every row for real inside the transaction and rolls it back, so the report is a rehearsal and not a guess. Reconciliation is scoped to `import:*` idempotency keys — reconciling against whole-table totals is correct exactly once, into an empty database, and reports a false FAIL forever after. Repeatable: a second run imports nothing.<br>- **Two new suites, 87 checks.** `test_repositories.js` (71) covers the seam, the legacy projections, pagination and the importer. `test_concurrency.js` (16) drives real OS processes, because `node:sqlite` is synchronous and an in-process concurrency test would prove only that a for-loop works: 40 concurrent sales → 40 distinct numbers with no gaps, 100 duplicate submissions → one invoice, 20 racing deposits on one UTR → one credit, and `process.exit()` at four separate write steps → nothing left behind.<br>- **FIVE REAL BUGS, all found by writing the tests rather than by reading the code.** (1) `advanceRepository.transition()` bound named parameters its SQL did not reference on the reject path, so **every rejection threw and left the claim pending** while telling the cashier it had saved. (2) `uq_advance_entries_reference` burned a payment reference even after the claim was rejected, so a customer who mistyped a UTR could never resubmit it — fixed by migration `003`, restoring the rule the JSON layer actually enforced. (3) **Document numbers could collide across financial years**: the suffix came from the calendar year while the sequence resets per financial year, so FY 2025-26 and FY 2026-27 both rendered sequence 1 as `-26`. Now derived from the financial year in one shared `documentNumber()` helper, with the importer additionally reserving the series each imported number implies. (4) The importer's pre-write backup used `copyFileSync` on a **WAL** database, which omits everything committed since the last checkpoint — the operator's safety net was a stale snapshot. Now `checkpointAndCopy()`. (5) `busy_timeout` was applied AFTER `journal_mode = WAL`, but switching to WAL takes a brief exclusive lock — so a simultaneous open died instantly with "database is locked" and no timeout configured yet. The concurrency suite failed ~1 run in 3 until the PRAGMA moved to the top of the list; this would have bitten a redeploy or a hand-run migration in production.<br>- **Migrations `002` and `003`** added rather than editing `001`, because `migrate.js` refuses an edited already-applied migration and that refusal is the mechanism working, not an obstacle to route around. | - [backend/repositories/](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/) (10 repositories + `calendar.js` + migrations `002`, `003`)<br>- [backend/services/](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/services/)<br>- [backend/importLegacyJson.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/importLegacyJson.js)<br>- [backend/test_repositories.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_repositories.js)<br>- [backend/test_concurrency.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_concurrency.js)<br>- [backend/package.json](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/package.json) |
| 2026-08-13 | -- | AI Assistant | **Phase 27: the four security gaps Phase 25 left open — hashed PINs, session revocation, privileged MFA, refund thresholds**<br>- **PINS ARE NO LONGER STORED IN THE CLEAR.** The master PIN always sat in `settings.json` as plaintext and the Phase 25 operator roster inherited that, so anyone who could read a backup, a support export or the tenant’s disk could read every credential on the terminal. All of them are now scrypt hashes in the SAME self-describing `scrypt$N$r$p$<hex>` format `customerAuth.js` already used for customer passwords — one hashing mechanism in the project, not two. `migratePinsToHashes()` converts an existing tenant on its next boot and deletes the plaintext; it is idempotent, and `verifyAdminPin` runs it lazily too so a process that imports the app without booting it still works. **One tenant-wide `authSalt`, deliberately:** a PIN-only login has no username to look a per-user salt up by, so a per-operator salt would cost one scrypt call per operator per attempt (five seconds at 50 staff). The consequence — equal PINs hash equally — is safe only because duplicate PINs are already refused, and that is now enforced by comparing hashes. The salt and every hash are masked out of `GET /api/settings` and the support export.<br>- **A DESIGN FLAW FOUND AND FIXED WHILE DOING IT.** `DEFAULT_SETTINGS` held `adminPin: "1234"`, and `getDefaultSettings()` is merged over a tenant’s settings on *every* boot — so the migration hashed the PIN, deleted the plaintext, and the next boot merged the plaintext default straight back in beside the hash. The hashing appeared to work and quietly undid itself. **No credential may live in that template**; the default is now *seeded* by `migratePinsToHashes()` when no hash exists at all, and CLAUDE.md §0 records the rule.<br>- **SESSION REVOCATION.** A 12-hour session used to be reachable only by its own holder’s logout, so removing somebody from the roster or changing their PIN after they walked out with it did nothing until it aged out — the credential was gone but the access was not. Deactivating, removing, re-PINning or **demoting** an operator now ends their live sessions inside the same request (`revokeSessionsForRosterChange`, after the write, sparing the caller’s own token so an owner can rotate their own PIN without signing themselves out). A role change counts because a demoted manager’s open session would otherwise keep its approving role for the rest of the day. An owner or manager can also see every live sign-in and end one deliberately (`GET /api/admin/sessions`, `POST /api/admin/sessions/revoke`) — **tokens never appear in that listing**, only opaque SHA-256 handles, and nobody can revoke their own session from it.<br>- **PRIVILEGED MFA (TOTP), stdlib only.** RFC 6238 over HMAC-SHA1 via `node:crypto` — **no dependency added**; the enrolment QR is rendered by the `qrcode` package already in the budget, server-side, so no secret goes into an `<img src>` pointing at a third party. Enrolment is two-step and **requires a live code before anything is stored**, so a secret nobody actually holds cannot be enrolled. **Ten single-use recovery codes** are issued once, stored only as hashes, and consumed on use — not optional, because a lost phone must not lock a shop out of its own till. With `requireMfaForApprovers` on, releasing money needs a session that passed the factor, which the shared master PIN cannot do by design (there is no person to enrol) — that refusal is what moves a store onto named logins. A wrong code counts as a failed login against the existing lockout, so a correct PIN does not buy unlimited guesses at six digits.<br>- **REFUND APPROVAL THRESHOLD.** A cash refund is the one counter action that takes money out of the till on a cashier’s own say-so, and it was the obvious remaining insider-fraud gap once roles existed. `refundApprovalThreshold` refuses a refund at or above a configured amount unless an owner or manager authorises it — checked against the amount the **server** priced, not one the client proposed, and after pricing but before any write. 0 disables it, which is the previous behaviour and therefore the default, so no existing store meets a refusal it never configured.<br>- **UI.** The lock screen gains a code field that appears only when the PIN entered belongs to somebody enrolled (and keeps the PIN typed, rather than making them start again), plus a recovery-code path and inline errors instead of `alert()`. Settings → Staff & Roles gains a Two-factor column with per-person set-up/turn-off, a **Controls on releasing money** block for the threshold and the MFA switch, and a **Who is signed in right now** table with per-session sign-out. Turning the MFA requirement on is refused client-side when no active owner or manager is enrolled — otherwise a store locks itself out of its own approvals.<br>- **A MISTAKE I MADE, AND THE RULE THAT CAME OUT OF IT.** I statically imported `adminAuth.js` at the top of `test_http.js`. ESM hoists imports above the `process.env.GOLD_POS_DATA_DIR = …` lines, so `db.js` resolved `DATA_DIR` to the real `backend/data` and the suite booted against the live tenant, migrating its `settings.json` before a 401 gave it away. **Repaired:** the two keys my run added were removed, leaving the plaintext `adminPin` already on disk as the source of truth (a snapshot was taken first, and the file now differs from `backups/backup_2026-08-10` only by the three additive keys the ordinary defaults merge adds). **Prevented:** both HTTP suites now load auth helpers through a dynamic `await import()` after the env is set, `test_routes.js` unsets the vars afterwards because `GOLD_POS_DATA_DIR` outranks the `GOLDPOS_DATA_DIR` its child spawn passes, and a full `npm test` is verified byte-for-byte not to touch `backend/data`. CLAUDE.md §8 now names the static-import case specifically.<br>- **Tests: 309 → 403 checks.** `test_suite.js` gains Test 8 (PIN hashing: migration, deletion, idempotence, fresh-install seeding, refusal when no salt exists, recovery-code single use) and **Test 9 — TOTP against the published RFC 6238 vectors**, which is the only check that proves a real authenticator app will interoperate; every other MFA test presents a code from our own generator, so without it a consistent bug would pass everywhere and fail on a cashier’s phone. `test_http.js` 68 → 80 (hashes never reach the browser, revocation on PIN change and on deactivation, the session list and its refusals, enrolment requiring a live code, recovery-code replay, the MFA approval gate, the refund threshold either side of the line, threshold validation). `test_routes.js` 27 → 28, its plaintext-PIN assertions replaced with the stronger pair "the PIN still verifies" and "no plaintext survives on disk". All 43 Playwright journeys still pass. **Verified end-to-end on a throwaway tenant** whose settings began with three plaintext PINs: the boot migration removed them and all three still signed in; changing a PIN killed two live sessions; a cashier was refused a ₹7,725 refund by name and the owner filed it; enrolment refused a wrong code then issued ten recovery codes; the manager needed a code to sign in; a recovery code worked once and not twice; the master-PIN owner was refused an approval for carrying no second factor while the enrolled manager was allowed. | - [backend/adminAuth.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/adminAuth.js)<br>- [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/defaultSettings.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/defaultSettings.js)<br>- [backend/productionGuard.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/productionGuard.js)<br>- [frontend/index.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/index.html)<br>- [frontend/js/app.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/app.js)<br>- [frontend/js/components/SettingsManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/SettingsManager.js)<br>- [backend/test_suite.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_suite.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js)<br>- [backend/test_routes.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_routes.js)<br>- [CLAUDE.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CLAUDE.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/PRODUCTION_READINESS_ROADMAP.md)<br>- [docs/TESTING_CHECKLIST.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/TESTING_CHECKLIST.md)<br>- [docs/ai_handover.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/ai_handover.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md) |
| 2026-08-13 | -- | AI Assistant | **Phase 28: Adversarial hardening pass — settings type validation, advance-sign floors, Settings-screen escaping**<br>Deliberate "try to break it" sweep. Four purpose-built harnesses (money-math property fuzzer over ~120k generated invoices; concurrency/double-spend; customer-portal & payment auth; settings type-confusion) plus all eight suites and the 43 Playwright journeys. **The transactional core held everywhere it was attacked** — 25 concurrent sales produced 25 unique invoice numbers with no lost writes, concurrent full returns refunded exactly once, concurrent advance redemption double-spent nothing, no prototype pollution via `/api/settings` or `/api/sales`, no path traversal, no IDOR or privilege escalation across customer/admin/cashier boundaries, webhook and payment signatures fail closed, login rate limiting engaged. Ten real defects were found off that core, all fixed:<br>**1. `POST /api/settings` had no type validation** (root cause of five defects; it spreads `...req.body` and the billing pipeline reads those keys with plain JS coercion). `invoiceSeqStart: "10"` made `startSeq + 1` a string **concatenation**, running the strictly-sequential invoice series 10 → 101 → 1011 → 10111; `"abc"` filed invoice `GOLD-000abc-26` and bypassed the lower-the-sequence guard because `parseInt` returned NaN; `goldTaxSlab: "abc"` read as `Number(x) \|\| 0` and **silently billed 0% GST**; `-50` and `1e9` were stored and billed (a ₹1,000 sale became ₹10,000,001,000); `invoicePrefix: {}` stamped `[object Object]` into permanent invoice numbers, and `{toString:'x'}` threw a 500 on **every** sale until settings were hand-edited. Fixed with `SETTINGS_FIELD_RULES` + `validateSettingsPatch()` in `defaultSettings.js` — types declared beside the template that declares the keys, enforced at the one write choke point, and **canonicalised** (`"10"` is stored as `10`) because validating a value while still storing it stringified would leave the concatenation bug in place.<br>**2. Invoice numbering and tax-slab reads hardened in `server.js`** so a settings.json arriving from a restored backup or hand edit — which never passes the route validator — is coerced and **repaired on disk** rather than propagated, with the correction logged.<br>**3. `computeInvoiceTotals` could be made to INFLATE a bill.** A negative `customerAdvanceBalance` passed the `Math.min` and became the resolved advance; a negative `appliedAdvance` failed the `> 0` test, skipped the clamp entirely and survived to the subtraction. Both produced a total *above* `totalBeforeAdvance` — an overcharge with no line explaining it. Now floored at zero. Not reachable through `POST /api/sales` (which rejects both), but this module is where the arithmetic is defined and every other caller runs through it.<br>**4. A negative line value broke the rows-sum-to-header invariant** — the header summed it while `allocateLines()` clamped its weight to zero, so printed rows disagreed with the total beneath them. Now floored.<br>**5. Stored XSS in the Settings screen:** 12 free-text settings were interpolated into `value="..."` unescaped, while the same file's own `escapeHtmlAttr` was used correctly elsewhere; 6 numeric fields were interpolated uncoerced. All 18 fixed. Print/reprint/credit-note paths were already escaped correctly.<br>**Tests:** `test_billing_math.js` 140 → 145 (new §17 "Hostile inputs"), `test_http.js` 80 → 87 (new "Settings TYPE validation" block, including a poisoned-file repair check). Suite total **403 → 415, green, exit 0**; 43/43 Playwright journeys still pass. | - [backend/defaultSettings.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/defaultSettings.js)<br>- [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [frontend/js/lib/billingMath.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/lib/billingMath.js)<br>- [frontend/js/components/SettingsManager.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/SettingsManager.js)<br>- [backend/test_billing_math.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_billing_math.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js)<br>- [CLAUDE.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CLAUDE.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md)<br>- [docs/ai_handover.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/ai_handover.md) |
| 2026-08-15 | -- | AI Assistant | **Phase 29: the route cut-over — the SQLite seam goes live, and the multi-line gap it hid** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 1 — the half Phase 26 deliberately left out)<br>- **THE SEAM WAS NOT MERELY DORMANT; ON THE INVOICE PATH IT WAS BEHIND THE ROUTES.** Phase 26 built the repositories when an invoice held one gold item, and the Billing Desk then started filing genuine multi-line carts. So `toLegacySale()` flattened to `lines[0]` and emitted no `lines[]`, no `tenders[]` and no `actor`; `createSale()` accepted a single item; `returnService` refunded against `lines[0]` whatever the customer brought back; and `importLegacyJson` rejected a MIXED invoice outright, because a mixed cart honestly stores `purity: 'MIXED'` and `goldPricePerGram: 0` and both tripped its "corrupt row" checks. Cutting the routes over to that seam as it stood would have silently regressed invariants `test_billing_math.js` §16 asserts. **The widening was therefore a prerequisite, not a nicety** — and it is most of this entry.<br>- **Migration `004`.** Two facts had nowhere to live: `invoice_lines.rate_source` (per-line rate provenance — the header rollup says `auto+manual` on a mixed invoice, which is enough to notice one and useless for auditing WHICH item was overridden) and `invoices.discount_bp` (the invoice-level discount rate; deriving it from line 1 is right only while there is one line). Both additive with backfills, so an existing tenant migrates without a rewrite.<br>- **The projection now returns BOTH shapes at once, as CLAUDE.md §0 requires** — `lines[]` with each item's purity, weight, rate, making charge, discount and its allocated share of the taxable value and GST, *and* the flat scalar rollup every pre-multi-line reader still uses. `purity: 'MIXED'` and `goldPricePerGram: 0` are emitted where the lines genuinely disagree rather than "corrected" to line 1. A redeemed advance is deliberately EXCLUDED from `tenders[]`: the schema records it as a tender row (correct for a drawer close), but on the wire that array must sum to `totalAmount`, which is the figure after the advance came off — listing it would double-count the customer's own credit.<br>- **`createSale` takes N lines**, prices each at ITS OWN purity's store rate from one snapshot, and stores the per-line allocation `allocateLines()` guarantees sums back to the header. The single-item request shape is normalised into a one-line cart, so there is one arithmetic path and a one-line invoice still prices to the paise exactly as before — asserted directly against the two request shapes.<br>- **`returnService` returns the RIGHT line.** It now takes `lineNumber`, refuses to guess on a multi-line invoice (pricing a 22K return at an 18K line's rate refunds the wrong money, quietly), and measures against that line's own running counter rather than the invoice's — so returning the chain cannot consume the bangles' returnable weight.<br>- **A SECURITY BUG THE CUT-OVER WOULD HAVE INTRODUCED, CAUGHT AND CLOSED.** Operators live in `settings.json`; every accountability column on the ledger is a foreign key into `users`, which held only two bootstrap rows. The services default `actorUserId` to the owner — and `advanceService` gates posting a claim on `users.isApprover()`, where `owner` passes. Cutting over as-is would have let **every cashier pass the approver check that exists to stop a cashier releasing money to themselves.** Closed by `users.ensureActorUser()`, which maps the session's actor onto a real `users` row and refreshes role and active state, so a demotion in Settings reaches the `approvers` view instead of being cosmetic. An unrecognised role degrades to `cashier`, never upward.<br>- **The routes.** `server.js` now imports the four services and the repository index and reaches persistence only through them. Every ledger `readJSON`/`writeJSON` call site is gone, along with ~500 lines of read-modify-write they lived in. `settings.json` and `license.json` stay JSON on purpose (§0). `customerAuth.js` moved on its two storage functions alone, exactly as designed — the repository already spoke the legacy account shape, so twenty-odd call sites above them are untouched. `db.js#initDatabaseFiles` no longer seeds the five ledger documents: `readJSON(file, default)` WRITES its default, so leaving them would have recreated empty `advances.json`-shaped files on every boot that look like an intact ledger and would be imported as one.<br>- **Pagination and aggregates are now the database's.** `GET /api/sales` used to read every `sales_YYYY.json` off disk, concatenate the store's entire history and serialise the lot — the largest thing the server did, and it got slower every trading day. Period `totals` are summed in SQL over the whole matched set (never the page), sharing one filter builder with `search()` so a page and the figures above it cannot describe different rows; the advance liability `summary` deliberately ignores the date filter because a lifetime balance is not a property of the period being browsed. `/api/advances/customers` rolls up per customer in SQL — the tab could not previously be paged without reporting wrong balances.<br>- **`invoiceSeqStart` still means something.** The allocator moved to `document_sequences`, so an owner editing that key mid-year would have saved successfully and seen the next invoice ignore them. `POST /api/settings` now pushes the change through (`sequences.setNextValue`), still behind the existing `confirmDestructive` guard on lowering it.<br>- **The diagnostics export is bounded.** It used to encrypt the store's entire trading history into one payload; it is a diagnostic sample now (500 rows per ledger) and says so, with true totals alongside. A full extract is a backup, which is a different job with different handling.<br>- **Boot.** `initialiseLedger()` migrates, seeds the organisation, and — only when the database is empty and legacy JSON exists — runs the importer once, loudly, keeping the JSON files as the rollback path. **Verified against a copy of the live tenant's own data:** 4 migrations applied, 4 invoices / 4 advance entries / 1 payment order imported, reconciled `ok` on all nine measures including `salesTotalPaise 90811786`, with `backend/data/` itself untouched.<br>- **Two rollback tests were rewritten rather than deleted.** Both injected an `fs.renameSync` failure to break a multi-file JSON write; there is no multi-file write left to break. They now force genuine mid-SQL-transaction failures reachable over HTTP — a tender split that does not reconcile, and the MFA branch of the refund threshold, both of which fire after the sequence has been allocated and rows written — and additionally assert the allocated number was **rolled back rather than burned**, leaving no gap in a legally-sequential series.<br>- **Tests: 415 → 426, green, exit 0.** `test_repositories.js` 72 → 82 with a new §4b covering the multi-line invoice end to end (three items, per-line rates, the honest rollup, rows summing to the header in paise, reprint fidelity, per-line return targeting, per-line returnable weight, and the one-line-invoice-is-unchanged invariant). `test_http.js` moved off JSON-file assertions onto the repositories. Both HTTP suites now `closeDb()` before removing their temp directory — Windows refuses to unlink an open file, and the resulting EPERM was thrown from `finally` and **masked the real assertion failure underneath it.** | - [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/db.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/db.js)<br>- [backend/customerAuth.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/customerAuth.js)<br>- [backend/importLegacyJson.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/importLegacyJson.js)<br>- [backend/repositories/migrations/004_multi_line_invoice_fidelity.sql](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/migrations/004_multi_line_invoice_fidelity.sql)<br>- [backend/repositories/ (invoice, creditNote, advance, user, sequence)](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/)<br>- [backend/services/ (sale, return)](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/services/)<br>- [backend/test_repositories.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_repositories.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js)<br>- [backend/test_suite.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_suite.js)<br>- [backend/test_concurrency.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_concurrency.js)<br>- [CLAUDE.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/CLAUDE.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/PRODUCTION_READINESS_ROADMAP.md)<br>- [docs/LEDGER.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/LEDGER.md)<br>- [docs/ai_handover.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/ai_handover.md) |
| 2026-08-16 | -- | AI Assistant | **Phase 30: the end-to-end journeys catch up with the SQLite ledger** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 1 — the verification Phase 29 left stale)<br>- **23 of 43 Playwright journeys were failing, and failing misleadingly.** Phase 29 retired the JSON ledger, but every spec still asserted against `sales_2026.json`, `advances.json`, `returns_2026.json`, `payment_orders.json` and `customer_auth.json`. Those files are the importer's source on first boot and are never written again, so each assertion compared a journey's work against the frozen seed. Stale JSON parses cleanly, so the specs reported arithmetic bugs that did not exist — `expected 27500, received 103125`.<br>- **`backend/tests/e2e/readLedger.mjs` (new) + a `readLedger()` fixture helper.** Runs **out of process** because `connection.js` resolves `DB_FILE` from `DATA_DIR` once at import and ESM caches it — a Playwright worker running two tests would stay pinned to the first test's database and read the wrong tenant silently (the CLAUDE.md §8 trap, reached from a new direction). Projects through the repositories' own `projectSalesPage` / `toLegacyAdvances` / `toLegacyOrder` rather than re-deriving the wire shape (§1, never a parallel third way).<br>- **Reads are bounded at the current instant.** `seed.js` deliberately builds a second trading year, so `sales_2027.json` rows are future-dated and sort first under `ORDER BY issued_at DESC`; `[0]` was returning a seeded 2027 invoice rather than the row the journey had just filed.<br>- **Two assertions had become vacuous and now test something.** They checked that `settings.json`'s `invoiceSeqStart` had not advanced, to prove a refused sale burned no invoice number. Phase 29 made `document_sequences` the allocator and left `invoiceSeqStart` as only its seed, so the value can no longer move and the assertions passed whether or not the sale was refused. They now assert nothing was filed, and that the next good sale takes the number the blocked attempt would have taken.<br>- `settings.json` reads are untouched — configuration stayed JSON on purpose (§0). **43/43 green, 4.5m.** | - [backend/tests/e2e/readLedger.mjs](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/readLedger.mjs)<br>- [backend/tests/e2e/fixtures.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/fixtures.js)<br>- [backend/tests/e2e/cashier-billing.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/cashier-billing.spec.js)<br>- [backend/tests/e2e/customer-portal.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/customer-portal.spec.js)<br>- [backend/tests/e2e/reprint-desk.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/reprint-desk.spec.js)<br>- [backend/tests/e2e/return-desk.spec.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/tests/e2e/return-desk.spec.js) |
| 2026-08-16 | -- | AI Assistant | **Phase 31: the audit trail becomes readable** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 2 — the strand the insider-fraud note named)<br>- **`GET /api/audit` (new, `server.js`).** Filterable by record type, entity, actor, action and date, paged through the same `parseLedgerQuery()` every other ledger route uses, newest first, with `detail_json` parsed server-side so the browser never has to `JSON.parse` a nullable column.<br>- **Approver-only, on `requireApprover`** — the same gate that governs releasing a claim. The trail names who released money, which makes it the record a cashier under suspicion has the strongest motive to read and the least business reading; keeping both questions on one rule avoids two answers drifting apart.<br>- **`frontend/js/components/AuditTrail.js` (new) + an Audit Trail tab.** Follows the `CustomerAccountsManager` pattern (its own `escapeHtml`, `advances-table` markup, `adminFetch`). A 403 renders as an explanation rather than an empty table, because "you may not see this" and "nothing happened" must not look alike. Deliberately **not** in `app.js`'s boot-time refresh block: the tab is approver-only, so refreshing on login would fire a 403 for every cashier.<br>- **Why this existed to be built:** `audit_events` has been append-only by trigger since Phase 24 and written on every money path since Phase 29 (`saleService`, `returnService`, `advanceService`, `paymentService`), but nothing exposed it. An append-only table nobody can read is evidence in principle and not in practice — the trigger stopped it being edited, and the absence of a route stopped it being used.<br>- **+4 checks in `test_http.js`** (87 → 91; suite total 426 → 430): the approver read with newest-first ordering and parsed detail, the cashier's 403, the anonymous 401, and date validation. The cashier check signs in with `43219`, not `4321` — the session-revocation test earlier in the suite rotates that PIN, and this runs after it.<br>- **Still open on this roadmap line:** PII classification, retention, clock sync, alerting and tamper-evident export. The trail cannot be edited; nothing yet proves to a third party that it has not been. | - [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [frontend/js/components/AuditTrail.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/components/AuditTrail.js)<br>- [frontend/index.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/index.html)<br>- [frontend/js/app.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/app.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js) |
| 2026-08-16 | -- | AI Assistant | **Phase 32: the operational boundary — readiness, draining, request identity and safe errors** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 3, plus the "safe errors" half of a Phase 2 line)<br>- **Readiness split from liveness.** `GET /api/health` is now documented and tested as liveness *only* — it deliberately touches no dependency, so a restart supervisor reading it never kills a process for a fault restarting cannot fix. New **`GET /api/ready`** answers 200 when the ledger opens, answers a query and is fully migrated, and 503 naming the failing check otherwise. Public and license-gate-exempt, because the prober has no credentials and a lapsed licence must stop selling without making the fleet look down.<br>- **`repo.dataStoreHealth()` (new, `repositories/index.js`)** carries the detail; `isDataStoreReady()` is now `dataStoreHealth().ok`, so there is one probe rather than two. The database check is a real query (`getTenant()`), not a handle null-check — a handle whose file was deleted underneath it still looks open. No SQL rose above the seam (ADR-001 §3).<br>- **Graceful shutdown (`shutdown()` in `server.js`, SIGTERM/SIGINT).** Readiness flips to 503 **first**, the listener closes **second**, the ledger handle closes **last, after the final response** — reverse those and every deploy drops a handful of requests, or a repository write outlives its handle where a cashier can see it. Idle keep-alive sockets are closed immediately (they hold the listener open for nothing); anything still open after `SHUTDOWN_GRACE_MS` (10s) is forced. Idempotent.<br>- **`kill_timeout: 15000` in both PM2 configs** — load-bearing, not tidying: PM2 SIGKILLs 1600ms after SIGINT by default, which would kill the process mid-drain and make all of the above decorative.<br>- **Request identity.** One id per request, echoed as `X-Request-Id` and added to `exposedHeaders` so a split-origin frontend can actually read it. An inbound id is reused (that is what makes a trace span the proxy hop) but only if it matches `[A-Za-z0-9._-]{1,64}`: the value lands in a log file, and an unvalidated header could inject newlines and forge whole entries. Anything else is discarded rather than sanitised — a half-scrubbed id correlates to nothing.<br>- **The middleware sits ABOVE the body parsers.** `express.json()` rejects malformed and oversized bodies by throwing, so an id assigned after it is not yet assigned for exactly the requests most worth tracing. The suite caught this: the 400 came back `requestId: "unassigned"`.<br>- **Structured logs.** `logTelemetry()` merges a `fields` object (`requestId`, `method`, `path`, `statusCode`) into the JSON line it already wrote; `logError()` appends a context object to its message line. Both additive — existing callers produce byte-identical output. `path` is `req.path`, never `originalUrl`, because a query string can carry a phone number.<br>- **Safe errors: the first terminal error handler this app has had.** Anything thrown outside a route try/catch previously reached Express's default handler, which renders the **stack trace into the response body** outside `NODE_ENV=production`. Now: a fixed message plus the request id for a 500, the parser's own status honoured for a bad body, stack to `error.log`. An unmatched `/api/*` answers a JSON 404 instead of the HTML one a `fetch()` cannot parse.<br>- **Found by curling the running server, not by the suite:** mounting that 404 handler at `/api` stripped the prefix off `req.url` and — because the handler never calls `next()` — Express never restored it, so the body *and every 404 telemetry line* named a path that does not exist. Rewritten as a predicate; asserted now.<br>- **The three `cd-*.yml` smoke tests poll `/api/ready`** instead of sleeping 3s and curling liveness, so a half-migrated deploy fails the gate rather than passing it and serving errors to the counter.<br>- **+7 checks in `test_http.js`** (91 → 98; suite total 430 → 437). **Not covered:** signal delivery itself, which Windows cannot emulate (`process.kill` there is an unconditional terminate) — the drain is tested by calling `shutdown()` directly, and the signal wiring is exercised only on Linux. | - [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/db.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/db.js)<br>- [backend/repositories/index.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/index.js)<br>- [backend/repositories/connection.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/connection.js)<br>- [backend/licenseChecker.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/licenseChecker.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js)<br>- [deploy/ecosystem.base.cjs](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/deploy/ecosystem.base.cjs)<br>- [deploy/ecosystem.config.cjs](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/deploy/ecosystem.config.cjs)<br>- [.github/workflows/cd-dev.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-dev.yml)<br>- [.github/workflows/cd-sandbox.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-sandbox.yml)<br>- [.github/workflows/cd-live.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-live.yml) |
| 2026-08-17 | -- | AI Assistant | **Phase 33: the request boundary — abuse limits and runtime shape checking** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 2 — the two items that line called "genuinely still open")<br>- **`backend/rateLimit.js` (new).** One bounded keyed counter (`createBoundedMap`) plus the middleware factory built on it. **It replaces nothing and duplicates nothing:** two hand-rolled attempt trackers already existed (`failedAttempts` in `adminAuth.js`, `ipFailures` in `customerAuth.js`), and both now store their counts in this map while keeping their own escalation policy. A credential lockout counts *failures* and escalates; an abuse limiter counts *requests* in a window. Different policies, one substrate, no third way to count per IP (§1).<br>- **A real leak fixed on the way.** Both trackers used plain Maps that only ever shed an entry on a **successful** login — so every source that failed once and never came back stayed resident for the life of the process, and one request per new IP grew them without bound. The bounded map expires entries and, if a flood outruns the sweep, drops oldest-first. TTL is deliberately **24h, not the 15-minute lockout cap**: expiry resets the count, and the count is what makes the cooldown escalate, so a short TTL would hand an attacker five fresh attempts every lockout.<br>- **Blanket limit: 600/min per IP on `/api/*`**, high enough never to be the reason a sale cannot be filed. **Exempt:** `/api/health` and `/api/ready` (monitoring polls them by design; throttling a probe makes a healthy process look down and can trigger a false failover) and `/api/payment/webhook` (Razorpay has already taken the money by the time it calls — a 429 would only lose our record of it, and the endpoint is HMAC-signed).<br>- **Named limits where every request succeeds and the abuse is the volume:** registration 10/h, password reset 5/15min, deposit claims 20/h, payment orders 30/h, and one shared 12/h across the expensive admin operations (price sync, backup, send-report, and the two encrypted exports). Each is far above real counter usage and far below what makes the endpoint a weapon. Registration carries **both** limiters, because `customerLoginRateLimiter` counts failures and so never fires on a flood of *successful* registrations — which is exactly what account spam is.<br>- **`backend/validation.js` (new) is `validateSettingsPatch()`'s engine lifted out, not a second validator.** `validateSettingsPatch()` is now a two-line caller over the same `validateAgainstRules()`; the ~60 lines of duplicated type/range/pattern logic are gone from `defaultSettings.js`. `validateBody()` applies the same rules to a request body and writes the **canonical** values back, so a handler reading `req.body` gets the right type rather than merely a checked one.<br>- **Schemas cover the credential surface only:** admin login, customer register/login/profile/password-change/forgot/reset. They check **shape** — types, lengths, character classes, presence — and never meaning: `isValidPhone()` still decides what is dialable and `validatePasswordStrength()` what is strong. The money routes are deliberately left alone; their services already validate exhaustively and a thinner second check there would add risk, not coverage.<br>- **The admin PIN rule is a string rule with `preserveWhitespace`, and that is load-bearing** — coerced to a number, `"0421"` becomes `421` and a leading-zero PIN silently stops matching its own hash.<br>- **A malformed body must not spend the lockout budget.** The schema runs after the rate limiter but before the handler, so no failed attempt is recorded; eight junk logins in a row are eight 400s, never a 429. Otherwise anyone could lock a store out of its own counter with junk JSON. Asserted.<br>- **`validation.js` imports nothing**, deliberately: `defaultSettings.js` now depends on it and is the one module a suite may import statically (§8), so a path to `db.js` here would pin every such suite to the live tenant.<br>- **THE BUG THIS SHIPPED WITH FOR AN HOUR, AND WHAT CAUGHT IT.** `CODE_RULE`'s pattern sensibly demands at least one character — and `frontend/js/app.js` posts every field its form owns, so an operator with no second factor still sends `totpCode: ""` and `recoveryCode: ""`. The first version therefore **400'd every ordinary admin sign-in**. `npm test` was fully green through it, because the HTTP suite posts only `{pin}`; the **Playwright journeys, which drive the real form, are what failed**. Fixed at the engine rather than by loosening each pattern: an empty string for a non-required field now means "not supplied", which is what a browser form actually means by it. Skipping leaves `req.body` untouched, so handlers still see the `""` they always saw. Regression-checked by posting the browser's exact login shape.<br>- **+5 checks in `test_http.js`** (98 → 103) and **Test 10 in `test_suite.js`** (9 → 10 numbered tests); suite total 437 → 443. **No new dependency** — the 7-runtime-dep budget is untouched. | - [backend/rateLimit.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/rateLimit.js)<br>- [backend/validation.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/validation.js)<br>- [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/defaultSettings.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/defaultSettings.js)<br>- [backend/adminAuth.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/adminAuth.js)<br>- [backend/customerAuth.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/customerAuth.js)<br>- [backend/test_http.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_http.js)<br>- [backend/test_suite.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_suite.js)<br>- [docs/brain/brain.map.json](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/docs/brain/brain.map.json) |
| 2026-08-17 | -- | AI Assistant | **Phase 34 (in progress): cookie-based sessions + CSRF, and the backup engine's missing ledger** — closing roadmap items from `PRODUCTION_READINESS_ROADMAP.md` §5 Phase 2 (session transport/CSRF) and an urgent gap found while scoping Phase 3 (backups).<br>- **`backend/backupEngine.js` was silently excluding the SQLite ledger from every backup** — `createBackup()` only ever copied `*.json` files, so `gold-pos.db` was never backed up since the SQL cutover. Fixed by reusing the existing `checkpointAndCopy()` in `backend/repositories/connection.js` (already built, already tested, just never wired in) instead of a raw file copy, which would silently miss data still in the WAL file.<br>- **Session auth moved from a Bearer token in `sessionStorage`/`localStorage` to HttpOnly/Secure/SameSite=Lax cookies**, with double-submit CSRF (a paired non-HttpOnly `*_csrf` cookie, echoed as `X-CSRF-Token` on every mutating request, checked against both the cookie and the server-side session record). New `backend/cookies.js` (hand-rolled parse/serialize — no new dependency). `backend/adminAuth.js` and `backend/customerAuth.js` sessions each carry a `csrfToken` now (the customer one persisted, since those sessions survive a restart). CORS gained `credentials: true` against the existing exact-origin allowlist.<br>- **Full re-verification, not just `npm test`**: all 443 backend checks green, plus all 43 Playwright e2e journeys (desktop + mobile) — required per CLAUDE.md §8 for anything touching a form or an auth path, since the HTTP suites alone would not have caught a browser-shaped regression.<br>- Remaining for this phase: secrets-at-rest encryption + key rotation, audit-trail hardening (retention/PII doc/tamper-evident export), the rest of backup/PITR/restore-drill/runbooks, and CI security scanning (SAST/secret-scan/dependency-review/SBOM). Plan: `.claude/plans/luminous-munching-eich.md`. | [backend/cookies.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/cookies.js)<br>- [backend/adminAuth.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/adminAuth.js)<br>- [backend/customerAuth.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/customerAuth.js)<br>- [backend/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/server.js)<br>- [backend/backupEngine.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/backupEngine.js)<br>- [frontend/js/app.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/js/app.js)<br>- [frontend/customer.html](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/frontend/customer.html) |
| 2026-08-17 | -- | AI Assistant | **Phase 34 (units 2–5 of 5): encryption at rest, a tamper-EVIDENT audit trail, restore verification, and CI security scanning** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 2 + Phase 3)<br>- **SECRETS ARE ENCRYPTED AT REST (`backend/secretVault.js`, new).** `redactSettings()` already kept credentials off the wire; the file itself still handed them over. The Razorpay secret, the SMTP password, the tenant `authSalt`, every operator PIN hash and every TOTP secret sat in `settings.json` as plaintext, so a stolen counter PC, a mis-scoped backup or a hand-assembled support bundle carried live keys. AES-256-GCM, fresh IV per value, format `encv1$iv$tag$ct` echoing the existing `scrypt$…` convention — **and the field's own dotted path as additional authenticated data**, so a ciphertext cut from `smtp.pass` and pasted over `razorpayWebhookSecret` fails authentication instead of decrypting happily.<br>- **THE KEY MAY NOT LIVE IN THE DATA DIRECTORY IN PRODUCTION.** A key stored beside the ciphertext it protects turns the exercise into obfuscation — whoever took the directory took both halves, which is precisely the loss this defends against. `GOLD_POS_SECRET_KEY` is the only accepted source under `NODE_ENV=production`; outside it, a 0600 keyfile is generated on demand so the dev loop and the suites need no ceremony, and it says loudly in the log that it is not a control.<br>- **Boot order turned out to be load-bearing.** The production guard reports every blocker at once, but `bootstrapServer()` calls `migrateStoredPins()` **first** — deliberately, because the guard asks "is the master PIN still the default?" and can only answer against the canonical stored form. Reading settings now means decrypting them, so a keyless production install died inside the PIN migration with a raw stack trace instead of the numbered refusal an operator can act on. Split out as `assertVaultKeyReady()`, which runs before anything reads settings; `assertProductionReady()` keeps the same check as defence in depth.<br>- **One door in and out (`backend/settingsStore.js`, new).** `settings.json` was read in **30 places across 8 files**, each calling `readJSON(path.join(DATA_DIR, …))` directly. Fine while it was plaintext; fatal once it is not — one reader that skips decryption hands the Razorpay client a string starting `encv1$`, and one writer that skips encryption silently returns a live secret to the clear and undoes the control for that field forever. The choke point CLAUDE.md §1 asks for, created rather than found. `db.js#migrateSettings()` is the one deliberate exception: it merges the template and writes back without ever touching a credential (§0 forbids one in `DEFAULT_SETTINGS`), so it round-trips ciphertext untouched and stays on raw `readJSON`, which also avoids an import cycle.<br>- **Migration is lazy, so there is no flag day.** `open()` returns anything not in `encv1$` form untouched, so an existing tenant keeps working and is sealed on its next write. Sealing is idempotent and an empty credential stays empty — sealing `''` would make the Settings screen claim a credential exists where none does.<br>- **Rotation is an operator task, not a button (`backend/rotateSecretKey.js`, new; `npm run key:rotate`).** It needs both keys at one moment and the server stopped, or a running process writes a value back under the old key and leaves one document encrypted under two. It verifies the rotated document reads back under the new key **before** replacing the original, and leaves a timestamped pre-rotation copy (gitignored). Exercised end to end on a throwaway tenant: seal, confirm no plaintext survives on disk, then rotate again with the new key active.<br>- **THE AUDIT TRAIL IS NOW TAMPER-EVIDENT, NOT MERELY APPEND-ONLY (migration `005`).** The 001 triggers stop the *application* rewriting history and prove nothing to a third party, because `DROP TRIGGER` is one statement for whoever holds the `.db`. Each event now carries `chain_seq`, `prev_hash` and `row_hash` = SHA-256 over its own fields plus the previous hash. **Pre-005 rows are deliberately NOT backfilled** — hashing them today would prove nothing about what they said then while looking authoritative — and `verifyChain()` reports their count as "predating the chain" rather than folding them into the verified total.<br>- **The honest limit is documented and asserted, not hidden.** Whoever holds the file can edit a row and recompute the whole tail, after which the chain verifies against itself. `test_repositories.js` §13 asserts exactly that, then asserts what defeats it: the re-hashed chain cannot reproduce a **head hash already in somebody else's hands**. So the control is the chain *plus the habit of exporting it*. All six checks tamper the way an attacker would — dropping the triggers and editing with direct SQL.<br>- **Reading the evidence: `GET /api/audit/verify` and `GET /api/audit/export`**, both on the same `requireApprover` gate as the trail itself, and **exporting is itself an audited event** — taking a copy of the evidence belongs in the evidence. A date range narrows the rows but never the manifest: a head hash over a chosen slice would pin nothing. `backend/verifyAuditChain.js` (`npm run audit:verify`) does it from the command line, with `--expect-head` for the comparison that actually settles a dispute.<br>- **A backup nobody has restored is a hypothesis (`backend/verifyBackup.js`, new; `npm run backup:verify`).** Restores the latest snapshot into a temp directory — never touching the live install — and runs nine checks: files and ledger present, SQLite `integrity_check`, migrations fully applied, business records actually there, **every invoice still summing to its own lines** (against `taxable`+`tax`, not the header total, which has the redeemed advance taken off it), the audit chain verifying, and whether the restored `settings.json` **decrypts on this host**. Exits non-zero on any failure. Both paths exercised against a real seeded snapshot: clean verifies 9/9; one sealed with an unavailable key fails on exactly that check with an intact ledger — **the new trap, because a snapshot deliberately carries sealed secrets and never the key, so a restore without `GOLD_POS_SECRET_KEY` is an intact ledger nobody can log into.**<br>- **`docs/RUNBOOKS.md` (new) — 11 procedures**, eight of the nine the roadmap names plus three the vault made necessary (rotate the key, recover from a lost key, prove the trail). Missing: incident response, left open because it overlaps the Phase 2 line requiring counsel review and writing a procedure a lawyer then rewrites is wasted work.<br>- **`docs/AUDIT_AND_PII.md` (new)** — a five-class scheme (P0 public → P4 accountability) with every field placed, who may read what, the integrity limits above, and a checklist for adding a field. **Retention is `[needs design decision]` and deliberately unstarted:** it is Indian tax/company law and the tenant's insurer, not engineering, and it collides with the chain — `trg_audit_events_no_delete` refuses a DELETE outright, so a retention job needs an archive-then-prune path recording a checkpoint hash, or verification reports a gap forever after.<br>- **CI security scanning added to `daily-checks.yml` — and NOT ticked, because it has never run.** Three jobs: TruffleHog (`--only-verified`, so an example key in a fixture cannot redden the gate permanently), Semgrep OSS at ERROR severity, and CycloneDX SBOMs via `npm sbom` — built into npm 10+, so no tooling and no dependency. **Every tool chosen to be free on a private repository:** CodeQL, `dependency-review-action` and SARIF upload all need GitHub Advanced Security there, so none is used; dependency vulnerabilities stay covered by the existing `npm audit` job. Unverifiable locally — no YAML parser on this machine, and these only execute on a runner, which still needs a PR.<br>- **Found and deliberately NOT fixed:** `backend/emailReporter.js` still reads the retired JSON ledger (`sales_*.json`, `returns_*.json`, `advances.json`), so since Phase 29 **the daily email report has been built from frozen import-time data** — the same class of defect Phase 30 found in the e2e specs. Outside these four units; wants its own change with its own tests.<br>- **Tests: 443 → 456, green, exit 0**, and **43/43 Playwright green** (mandatory here, not a formality — unit 1 rewrote how every settings read reaches the auth path). `test_suite.js` +Test 11 (vault round trip, empty-stays-empty, lazy passthrough, wrong key, field binding, wildcard paths, rotation); `test_repositories.js` 82 → 88 (§13 tamper evidence); `test_http.js` 103 → 107 (verify, export, export-is-audited, approver-only); `test_routes.js` 28 → 29 (**no canary secret survives in the file bytes**); `test_production_guard.js` 16 → 17 (a keyless production boot refuses with a blocker, not a stack trace). | - [backend/secretVault.js](backend/secretVault.js)<br>- [backend/settingsStore.js](backend/settingsStore.js)<br>- [backend/rotateSecretKey.js](backend/rotateSecretKey.js)<br>- [backend/verifyAuditChain.js](backend/verifyAuditChain.js)<br>- [backend/verifyBackup.js](backend/verifyBackup.js)<br>- [backend/repositories/migrations/005_audit_hash_chain.sql](backend/repositories/migrations/005_audit_hash_chain.sql)<br>- [backend/repositories/auditRepository.js](backend/repositories/auditRepository.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/productionGuard.js](backend/productionGuard.js)<br>- [backend/adminAuth.js](backend/adminAuth.js)<br>- [backend/defaultSettings.js](backend/defaultSettings.js)<br>- [.github/workflows/daily-checks.yml](.github/workflows/daily-checks.yml)<br>- [docs/RUNBOOKS.md](docs/RUNBOOKS.md)<br>- [docs/AUDIT_AND_PII.md](docs/AUDIT_AND_PII.md) |
| 2026-08-19 | -- | AI Assistant | **Phase 34 follow-up: the emailReporter bug is fixed, and CLAUDE.md's own duplication is gone**<br>- **`backend/emailReporter.js#computeSummary()` no longer reads the retired JSON ledger.** Flagged in the previous row as found-and-deliberately-not-fixed; now rewritten to read through `backend/repositories/index.js` — `invoices.periodTotals()`, `creditNotes.periodTotals()`, `advances.periodTotals()`/`liabilitySummary()` — the same seam every route uses, instead of `readJSON`-ing `sales_*.json`/`returns_*.json`/`advances.json`, which nothing has written since the Phase 29 cutover. The daily/monthly business summary email now reports live figures instead of the frozen import-time snapshot.<br>- **New coverage: `test_repositories.js` §13 "Reporting"** (+2 checks). Deliberately poisons the three retired JSON files with a planted ₹9,99,999 figure and asserts `computeSummary()` never reports it — the regression this fix closes, made assertable rather than just fixed.<br>- **`CLAUDE.md` had corrupted itself: a ~285-line duplicate of its own §0–§8 was pasted mid-sentence into itself** (inside the "SECRETS IN settings.json ARE ENCRYPTED AT REST" bullet, splitting the word `` `encv1$` `` in two), doubling the file to 650 lines. Root cause not established — likely a bad edit in an earlier session — but every section header was confirmed to appear exactly once after the fix, and `git diff --stat` against `HEAD` returned to a plausible +13/-2 instead of the ~300-line diff the corruption produced.<br>- **Also verified locally, previously assumed impossible: the Unit-4 CI YAML.** PyYAML is available on this machine via the `py` launcher (`py -c "import yaml"`), which the prior session didn't find. All four `.github/workflows/*.yml` files parse cleanly; the three new security-scanning jobs were reviewed by hand. This proves the files are well-formed, not that the jobs succeed on a runner — that still needs an actual GitHub Actions run.<br>- **Tests: 456 → 458, green, exit 0** (`test_repositories.js` 88 → 90). `npm run test:e2e` not re-run — nothing here touches a request body, form or auth path (CLAUDE.md §8). | - [backend/emailReporter.js](backend/emailReporter.js)<br>- [backend/test_repositories.js](backend/test_repositories.js)<br>- [CLAUDE.md](CLAUDE.md)<br>- [docs/ai_handover.md](docs/ai_handover.md) |
| 2026-08-21 | -- | AI Assistant | **Phase 41 (unit 1 of 6): audit retention — archive-then-prune, flagged off** (`PRODUCTION_READINESS_ROADMAP.md` §2, the audit/PII retention line — one of six items the owner asked to have built now rather than wait on their legal/product sign-off: audit retention, the wastage model, PITR, old-gold exchange, an incident-response runbook, and gold savings schemes, each gated behind a settings flag defaulting off so disabled behaves exactly as if the module never existed)<br>- **`007_audit_retention_checkpoints.sql` (new) — one row per prune run.** Holds the `checkpoint_hash`/`pruned_through_chain_seq` the surviving chain re-anchors to, so `verifyChain()` can walk forward from a pruned boundary instead of assuming history starts at NULL.<br>- **`backend/repositories/auditRetentionRepository.js` (new) — the single privileged writer allowed to defeat `trg_audit_events_no_delete`.** Prunes only a CONTIGUOUS PREFIX of the chain — the boundary is the lowest `chain_seq` that must survive (`occurred_at >= cutoff`), so a row whose age is ambiguous simply stops the prune rather than ever opening a gap in the middle of the surviving chain. The trigger is dropped and recreated inside the SAME transaction as the delete, so a crash mid-prune rolls back to the trigger being in place, never leaves the table deletable.<br>- **`auditRepository.js#verifyChain()` now seeds itself from the latest checkpoint automatically** — a plain internal SELECT, zero signature change for any caller (the audit routes, `exportChain()`, `verifyAuditChain.js`). A tenant that has never pruned has no checkpoint row, so this is a no-op lookup and behaviour is byte-identical to before.<br>- **`backend/auditRetention.js` (new)** — nightly cron at 02:00 (after the 01:00 backup, ahead of the 06:30 alert checks), no-ops unless `auditRetentionEnabled` is true, same "read the setting, bail with a bare `return`" contract as `alerting.js#checkTlsExpiry()`. `auditRetentionDays` defaults to 2555 (~7 years) — an engineering placeholder long enough to be safe for common record-keeping practice, explicitly NOT the legal determination `docs/AUDIT_AND_PII.md` §5 still calls for.<br>- **New settings**: `auditRetentionEnabled` (boolean, default false), `auditRetentionDays` (integer, default 2555), both through the existing `SETTINGS_FIELD_RULES`/`validateSettingsPatch()` choke point — no manual guard needed, unlike the older boolean settings that predate the rules table's boolean support.<br>- **Verification.** `test_schema.js` +2 (the checkpoint table's `rows_pruned >= 0` CHECK); `test_repositories.js` +7 — a true no-op when nothing is eligible, a past/future-dated fixture that gives a deterministic prune boundary regardless of how much other chain history the rest of the suite has already written, the chain still verifying post-prune (seeded from the checkpoint), an idempotent re-prune, the no-delete trigger back in place afterward, and unchained pre-005 rows ageing out on `occurred_at` alone with no checkpoint needed. Full `npm test`: 9/9 suites green, exit 0. `npm run migrate:check-safety` clean.<br>- **Built in the same shared tree as a concurrent session's Phase 37–40 work** (lot inventory, cash shifts, quotes/holds, invoice delivery) — see that session's own Phase 38 row below for how the shared files (`server.js`, `repositories/index.js`, `test_schema.js`, `test_repositories.js`) were reconciled without either side's uncommitted work being lost. | - [backend/repositories/migrations/007_audit_retention_checkpoints.sql](backend/repositories/migrations/007_audit_retention_checkpoints.sql)<br>- [backend/repositories/auditRetentionRepository.js](backend/repositories/auditRetentionRepository.js)<br>- [backend/auditRetention.js](backend/auditRetention.js)<br>- [backend/repositories/auditRepository.js](backend/repositories/auditRepository.js)<br>- [backend/repositories/index.js](backend/repositories/index.js)<br>- [backend/defaultSettings.js](backend/defaultSettings.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_repositories.js](backend/test_repositories.js)<br>- [docs/AUDIT_AND_PII.md](docs/AUDIT_AND_PII.md) |
| 2026-08-21 | -- | AI Assistant | **Phase 42: SKU catalogue mechanics — barcode, HSN, hallmark/HUID, gross/net/stone weight** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5.1, the catalogue-metadata half of "Catalogue, multi-line sale, barcode/labels, HSN/hallmark and weight/stone/making model" that Phase 37's `006_lot_inventory.sql` reserved `sku_code` for and explicitly deferred)<br>- **`011_sku_catalogue.sql` (new) — five additive columns on `inventory_items`** (`hsn_code`, `gross_weight_mg`, `net_weight_mg`, `stone_weight_mg`, `stone_value_paise`) **and one on `inventory_lots`** (`hallmark_huid`, unique per tenant via a partial index). Item vs lot placement follows the precedent `purity` already set: gross/net/stone weight and HSN are the design's NOMINAL figures for the catalogue and a printed tag, exactly like `purity` — the ACTUAL weight sold is still whatever the scale reads at billing, untouched. `hallmark_huid` is the one exception, living on the lot instead: BIS assigns one HUID per physical article, never per design, correct when a lot represents one piece and a known simplification when opened in bulk, the same trade-off `006_lot_inventory.sql` already accepted for weight-only lots.<br>- **Cross-column invariants live in JS, not SQL** — SQLite refuses a CHECK added via `ALTER TABLE ADD COLUMN` if it references another column, so "net weight cannot exceed gross weight" and "stone weight cannot exceed gross weight" are enforced by a new `assertWeightsConsistent()` in `inventoryRepository.js`, called from both `createItem()` and `updateItem()` (the patch is merged against the existing row first, so a lone `netWeightMg` patch is still checked against whatever gross weight the item already has).<br>- **`inventoryRepository.js` gained `findItemBySku()`** — a scanner or a phone reading the printed QR types the barcode value into a field exactly like a keyboard, so "scan to find" needs only a lookup, no scanning logic of its own.<br>- **`GET /api/inventory/items/by-sku/:skuCode`** (new) plus the existing item create/patch/lot-open/stock routes extended with the catalogue fields, validated through the existing `validateBody()` choke point (`SKU_CATALOGUE_FIELDS`, shared between the create and patch schemas). Label printing reuses the existing public `GET /api/qrcode?data=` endpoint (already in the dependency budget for TOTP/UPI QR) rather than adding a barcode-symbology library — the QR encodes the SKU code, and a jeweller's shelf-tag text (HSN/gross/net/stone weight) prints alongside it.<br>- **`InventoryManager.js`**: the New Item form gained the five catalogue fields; a new Edit action (reusing the same form) lets an item created without catalogue detail have it added later — necessary, since most items will be created before a barcode is assigned; a new Label button opens a printable tag in a new tab (QR image + text, `window.print()` on load); the lots sub-table gained a Hallmark HUID column and the New Lot form a matching field; search now also matches barcode/HSN.<br>- **Deliberately NOT wired into the sale/invoice flow** — same boundary `006_lot_inventory.sql` already drew for stock decrement: auto-filling a Billing Desk line from a catalogue item would touch `computeInvoiceTotals`, the most-tested function in the tree, and needs its own dedicated pass. Wastage stays out of scope entirely, unchanged from the roadmap's own note that it has no field, helper or setting anywhere and needs a product decision first.<br>- **Verification.** `test_schema.js` +5 (negative gross weight refused, negative stone value refused, a NULL catalogue field accepted, two lots sharing a HUID within a tenant refused, HUID uniqueness confirmed scoped per tenant); `test_repositories.js` +10 (catalogue fields round-trip through `createItem`/`getItem`, fields left NULL when omitted, both cross-weight invariants refused on create AND on patch-merged-with-existing, `updateItem()` patches only what is passed, `findItemBySku()` scoped to tenant and null-safe on a miss/blank, `itemStockSummary()` carries the fields through, a lot records its HUID, a duplicate HUID refused); full `npm test` 9/9 suites green, exit 0, both before and after a concurrent session's Phase 41 additions landed in the same shared files. A real local HTTP boot against the live dev tenant (session cookies + CSRF): create → patch → cross-field 400 → barcode lookup hit/miss → stock summary → QR generation, then the test item deactivated to leave the tenant clean. A headless-Chromium run driving the real admin UI: New Item with every catalogue field → row shows the barcode → Edit pre-fills every field correctly → HSN updated and persisted → View Lots shows the catalogue-detail line → New Lot with a HUID → HUID visible in the lots table → Label opens a new tab with a working QR image, the barcode text and the HSN/weight rows, screenshotted; zero console errors from any Inventory interaction (one pre-existing, unrelated `describeSaleGoods is not defined` error was found on the Dashboard tab and is not part of this change).<br>- **Same concurrent-session situation as Phases 38–41** — Phase 41 (audit retention) was mid-edit in the same shared files (`server.js`, `repositories/index.js`, `test_schema.js`, `test_repositories.js`) throughout; every edit here was additive and re-verified against the other session's latest state after it changed underneath, rather than reverting or overwriting it. | - [backend/repositories/migrations/011_sku_catalogue.sql](backend/repositories/migrations/011_sku_catalogue.sql)<br>- [backend/repositories/inventoryRepository.js](backend/repositories/inventoryRepository.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_repositories.js](backend/test_repositories.js)<br>- [frontend/js/components/InventoryManager.js](frontend/js/components/InventoryManager.js)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-21 | -- | AI Assistant | **Phase 41 (unit 6 of 6, final): gold savings schemes — the full engine, flagged off** (`PRODUCTION_READINESS_ROADMAP.md` §6, closing the six-unit push this phase began — audit retention, wastage, PITR, old-gold exchange, the incident-response draft, and now this, the largest by far and deliberately built last)<br>- **`015_gold_savings_schemes.sql` (new) — four tables.** `gold_schemes` (scheme identity + terms); `gold_scheme_enrollments`, whose terms columns (`installment_count`/`bonus_installments`/`default_grace_days`/`early_closure_penalty_bp`) are SNAPSHOTTED from the scheme at enroll time and then trigger-enforced immutable — a later change to a scheme's terms can never retroactively alter an enrollment already in progress, which is literally what Phase 6's own exit criterion ("the legal-approved terms version is stored with each enrollment") asked for; `gold_scheme_transitions` (status history); `gold_scheme_installments`, append-only (trigger-enforced, matching `audit_events`/`inventory_movements`/`old_gold_exchanges`), each row locking its own gold-gram equivalent at the rate in force the moment it was paid.<br>- **`computeGoldSchemePayout()` (new, `frontend/js/lib/billingMath.js`) makes the one real design call this unit needed: how is the bonus valued?** In GRAMS, not rupees — `bonusInstallments` × the enrollment's own average grams-per-installment, so the bonus scales with what the customer actually paid in rather than a rupee figure re-converted at whatever the maturity-day rate happens to be. The whole accumulated total (real + bonus) is THEN converted to ₹ at the maturity-day rate — the rate at which the accumulated gold is actually being handed back as spendable credit. An early closure earns no bonus and forfeits `earlyClosurePenaltyPercent` of its grams before that same conversion.<br>- **`backend/services/goldSchemeService.js` (new)** — `enroll()`, `recordInstallment()`, `matureEnrollment()`, `closeEarlyEnrollment()`, `markDefaulted()`, each `users.isApprover()`-gated (the same authority bar a posted advance deposit already needs — enrolling a customer and taking their money both move a counter obligation) and wrapped in one transaction with `audit.record()`, directly mirroring `advanceService.js`'s shape throughout. `ensureDefaultScheme()` lazily snapshots ONE scheme per tenant from current settings the first time it is needed — there is no scheme-management UI this pass, matching the "placeholder terms" scope decision.<br>- **A real bug caught before it ever reached a test**: the first draft of `matureEnrollment()`/`closeEarlyEnrollment()` called `advances.ensureAccount({customerPhone: null, ...})` — that function looks up AND creates `advance_accounts` rows keyed by phone, never by id, so a null phone would have silently created a SECOND, unreachable account disconnected from the one a sale or an old-gold exchange already finds for that customer, making a scheme payout uncredited in practice despite reporting success. Fixed by resolving the customer's actual phone via `customers.getCustomerById()` before calling `ensureAccount()` — caught by re-reading the call, not by a failing test, which is exactly why the HTTP integration test below asserts the credited advance entry is findable and spendable rather than merely that the route returns 200.<br>- **Redemption reuses the platform's advance/credit machinery — zero new logic**, the same reuse Unit 4 (old-gold exchange) already established: maturity and closure both post an ordinary `advance_entries` deposit, spendable through the Billing Desk's already-existing Apply Advance button.<br>- **`markDefaulted()` is a status flag only — no money moves.** Refused unless the enrollment is genuinely overdue (no payment within `default_grace_days` of the last installment, or of enrollment if none was ever paid) — a deliberately narrow scope; what a store does about an actually-defaulted customer's paid-in balance is outside this engine, on purpose, absent real product terms for it.<br>- **New settings**: `goldSchemeEnabled` (boolean, default `false`), `goldSchemeInstallmentCount` (default `11`), `goldSchemeBonusInstallments` (default `1`), `goldSchemeDefaultGraceDays` (default `30`), `goldSchemeEarlyClosurePenaltyPercent` (default `0`) — an "11 + 1 free" structure typical of Indian gold-scheme practice, explicitly an engineering placeholder.<br>- **Six routes** under `/api/gold-schemes/*` (enroll, list, pay an installment, list installments, mature, close-early, default), all 404-shaped when the module is off. **`frontend/js/components/SchemeDesk.js` (new)** — enroll form, per-enrollment pay/mature/close/default actions; its own nav button (`#gold-schemes-nav-btn`) starts hidden in `index.html` and is shown only once the desk's own 404-probe confirms the module is on, the same "never existed until turned on" contract every flagged module in this phase follows. Settings UI: a "Gold Savings Schemes" block, explicit in its own copy that these are placeholder terms pending legal/CA review.<br>- **Verification.** `test_billing_math.js` §21 (+6: the gram-lock division, a zero/negative rate locking zero rather than dividing by it, the full bonus/payout arithmetic, early-closure forfeiture, a 100%+ penalty clamped rather than over-forfeiting, an uneven per-installment average still bonusing fairly). `test_schema.js` (+6: append-only installments, the terms-snapshot-immutable-but-status-mutable trigger exercised in both directions, an unknown status refused, a duplicate installment number refused, a zero-amount installment refused). `test_http.js` (+2: disabled 404s; enabled — enroll refused for a cashier, a 3-installment/1-bonus scheme paid in full, maturity refused at 2-of-3 paid, matures correctly into a **verified-redeemable** advance entry — the exact defect the bug above would have produced — a second maturity attempt refused rather than double-crediting, a separate enrollment closed early with the penalty correctly forfeiting the bonus, and a fresh enrollment correctly refused as not-yet-overdue for default). Full `npm test`: 9/9 suites green, exit 0. `npm run migrate:check-safety` clean at 15 files. Full `npm run test:e2e` desktop-chromium project re-run — a new nav button touches the shared sidebar every existing spec renders against, same precedent Phase 37 already set — **31/31 green in one unbroken run (3.6 min)**. `mobile-chromium` (customer-portal.spec.js only, unaffected by an admin-side sidebar change) already confirmed 12/12 earlier this phase. 31+12 = all 43 journeys green. | - [backend/repositories/migrations/015_gold_savings_schemes.sql](backend/repositories/migrations/015_gold_savings_schemes.sql)<br>- [backend/repositories/goldSchemeRepository.js](backend/repositories/goldSchemeRepository.js)<br>- [backend/repositories/index.js](backend/repositories/index.js)<br>- [backend/services/goldSchemeService.js](backend/services/goldSchemeService.js)<br>- [frontend/js/lib/billingMath.js](frontend/js/lib/billingMath.js)<br>- [backend/defaultSettings.js](backend/defaultSettings.js)<br>- [backend/server.js](backend/server.js)<br>- [frontend/js/components/SchemeDesk.js](frontend/js/components/SchemeDesk.js)<br>- [frontend/js/components/SettingsManager.js](frontend/js/components/SettingsManager.js)<br>- [frontend/js/app.js](frontend/js/app.js)<br>- [frontend/index.html](frontend/index.html)<br>- [backend/test_billing_math.js](backend/test_billing_math.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_http.js](backend/test_http.js)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md)<br>- [docs/SCHEME_MODULE_PLAN.md](docs/SCHEME_MODULE_PLAN.md) |
| 2026-08-21 | -- | AI Assistant | **Phase 41 (unit 5 of 6): the incident-response runbook — DRAFT** (`docs/RUNBOOKS.md` §13, closing `PRODUCTION_READINESS_ROADMAP.md`'s "nine runbooks" line — the one held back since Phase 34 because it overlaps the Phase 2 line requiring Indian counsel review, and a polished procedure a lawyer then rewrites wholesale is wasted work)<br>- **Doc-only — no code, no settings, no toggle.** A runbook is a document; "disable = behaves as though it never existed" has no meaning for prose, so this unit is structurally different from the other five in this phase.<br>- **Written explicitly as a DRAFT**, marked so at the top of the section and in the roadmap checklist (which stays `[~]`, not `[x]`, until counsel actually reviews it) — the point is to leave less for a lawyer to start from, not to pre-empt their review.<br>- **Five subsections**: detection/triage (keyed to the alert codes `backend/alerting.js` already raises, plus tenant/customer reports); containment (the reversible actions already available — ending sessions, rotating the vault key, a full stop — explicitly `[needs decision: who holds authority for the last one]`, left open rather than guessed at, the same posture CLAUDE.md §4 already takes on the PIN keyspace and dual-control threshold); evidence preservation (the one subsection with real machinery behind it already — an immediate audit export, suspending Phase 41's own audit-retention prune for the duration, a PITR/backup snapshot before any containment write); notification obligations (`[needs decision: CERT-In 70B / DPDP Rules applicability]`, explicitly refusing to guess); post-incident review (verify, confirm rotated credentials actually propagated, log it, feed gaps back into the runbook).<br>- **The drill log gained a `Type` column** (`drill` vs `incident`) so §13.5's own instruction to log a real incident there is actually followable — the table only had drill columns before. | - [docs/RUNBOOKS.md](docs/RUNBOOKS.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-21 | -- | AI Assistant | **Phase 41 (unit 4 of 6): old-gold exchange — engineering built, flagged off** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5 item 4 — "Exchanges and old-gold remain blocked on legal sign-off"; the owner decided to build the engineering now rather than wait, on the explicit understanding that GST/RCM treatment stays unresolved and must be settled before this is ever turned on for a live tenant)<br>- **`014_old_gold_exchanges.sql` (new) — `old_gold_exchanges`, append-only** (matching `audit_events`/`inventory_movements`/`cash_shifts`, not a status workflow: an exchange is a physical fact — this gold was weighed, tested and credited on this date — with nothing to transition). Records declared vs. TESTED purity (priced at tested, never declared), gross/net weight, the rate and the credit, all in the schema's usual paise/milligram scale.<br>- **Does NOT widen `advance_entries`.** SQLite cannot `ALTER` a CHECK constraint in place, and the only way to widen `payment_method`/`source` is a create-copy-drop-rename rebuild — which trips `checkMigrationSafety()`'s `DROP TABLE` scan, correctly, since that gate exists precisely so a live, heavily-tested table is not rebuilt lightly. Instead: a dedicated table for the exchange fact, which then posts an ORDINARY `advance_entries` deposit using method/source values that already exist (`other`/`counter`) — the existing advance/credit-redemption machinery (already wired into `computeInvoiceTotals()` via `appliedAdvance`/`customerAdvanceBalance`) applies the credit with ZERO new redemption logic.<br>- **`computeOldGoldCredit()` (new, `frontend/js/lib/billingMath.js`)** — net weight = gross × (1 − deduction%), credited at the TESTED purity's own already-configured store rate (this codebase prices 24K/22K/18K as three independent settings-configured rates, never a 24K figure scaled by a fraction — so there is nothing to scale here either). Deliberately silent on tax.<br>- **`backend/services/oldGoldService.js` (new)** mirrors `advanceService.js#recordDeposit()`'s posted path closely: gated behind `settings.oldGoldExchangeEnabled` (404 when off — the route behaves as though it never existed) and `users.isApprover()` (crediting a customer's balance is cash-equivalent, the same bar a posted advance deposit needs). The deduction percentage comes from settings, never the request body — same posture wastage already takes on its own percentage.<br>- **New settings**: `oldGoldExchangeEnabled` (boolean, default `false`), `oldGoldDeductionPercent` (number, 0–100, default `5`).<br>- **`POST /api/old-gold-exchanges`** (`requireAdminSession`; the service itself refuses a non-approver with 403). **Billing Desk**: a panel (hidden unless enabled) records an exchange and calls the desk's existing `lookupCustomerAdvance()` to refresh the balance display — the credit then redeems through the ALREADY-EXISTING Apply Advance button beside it, unchanged. **Settings UI**: an "Old-Gold Exchange" block under Tax & Invoice Numbering, folded into the existing Save Billing Settings action, explicit about the unresolved GST/RCM question in its own copy.<br>- **Verification.** `test_billing_math.js` §20 (+5: the deduction arithmetic, 0% and 100% edge cases, clamping outside 0–100, that two different purity rates on the same net weight produce different credits — proving the store-rate model, not a fraction of one 24K price). `test_schema.js` (+3: append-only via trigger, net-weight-exceeds-gross refused, an unsupported purity refused). `test_http.js` (+2: disabled answers 404 as though the route never existed; enabled requires owner/manager, values 10g at 10% deduction to exactly 9g/₹9,000 at the TESTED purity's pinned rate — not the declared one — posts exactly one advance entry, and that credit redeems in full against a real sale, proven with a dedicated fixture phone rather than the suite's shared one so "apply advance" — all-or-nothing by design — resolves to a value the test can assert on exactly). Full `npm test`: 9/9 suites green, exit 0. `npm run migrate:check-safety` clean at 14 files. `cashier-billing.spec.js` (6/6) re-run after the Billing Desk panel landed — full 43-journey run still blocked by the same environment interruption noted in the unit 3 row above; this unit's own touched spec confirmed green standalone. | - [backend/repositories/migrations/014_old_gold_exchanges.sql](backend/repositories/migrations/014_old_gold_exchanges.sql)<br>- [backend/repositories/oldGoldRepository.js](backend/repositories/oldGoldRepository.js)<br>- [backend/repositories/index.js](backend/repositories/index.js)<br>- [backend/services/oldGoldService.js](backend/services/oldGoldService.js)<br>- [frontend/js/lib/billingMath.js](frontend/js/lib/billingMath.js)<br>- [backend/defaultSettings.js](backend/defaultSettings.js)<br>- [backend/server.js](backend/server.js)<br>- [frontend/js/components/BillingDesk.js](frontend/js/components/BillingDesk.js)<br>- [frontend/js/components/SettingsManager.js](frontend/js/components/SettingsManager.js)<br>- [backend/test_billing_math.js](backend/test_billing_math.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_http.js](backend/test_http.js)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-21 | -- | AI Assistant | **Phase 41 (unit 3 of 6): point-in-time recovery — frequent local snapshots, flagged off** (`PRODUCTION_READINESS_ROADMAP.md` §3, the "encrypted off-site backup and point-in-time recovery" line — the PITR half, previously blocked on "picking the interval is an RPO decision nobody has taken")<br>- **`backend/pitr.js` (new)** picks the lower-risk of the two approaches that line itself names — frequent snapshots over hand-rolled WAL-frame archiving — because it reuses `connection.js#checkpointAndCopy()`, the same already-correct, already-tested checkpoint-then-copy path `backupEngine.js`'s nightly job already relies on. No-ops unless `settings.pitrEnabled === true` (default `false`), same "read the setting, bail with a bare `return`" contract as `alerting.js#checkTlsExpiry()`.<br>- **A snapshot's directory layout deliberately matches a nightly backup's exactly** (the same `*.json` files plus a `checkpointAndCopy()`'d ledger file) — so restoring and verifying one needs ZERO new code: the existing `npm run backup:verify -- --backup <pitr-snapshot-dir>` already works, because its `--backup` path only bypasses the `backup_*`-prefix auto-pick filter, never the restore/verify logic itself.<br>- **The 5-minute cron tick is fixed; `pitrIntervalMinutes` (default `15`, matching this section's own already-stated ≤15-minute RPO target) is enforced INSIDE `archivePitrSnapshot()`** via an in-memory `lastArchivedAt` timestamp, not by generating a dynamic cron expression — the same idiom `alerting.js`'s per-code cooldown already uses. A value below 5 is accepted (not refused at the boundary) but has no effect, since the tick itself cannot go faster; documented rather than silently rounded.<br>- **New settings**: `pitrEnabled` (boolean, default `false`), `pitrIntervalMinutes` (integer, min `5`, default `15`), `pitrRetentionHours` (integer, default `24`).<br>- **Still explicitly local-only** — this closes the "how often" half of PITR, not the "survives losing this machine" half. Off-site shipping has no destination (no VPS or object store provisioned, per this project's own docs) and stays a follow-on.<br>- **Verification.** `test_suite.js` Test 13 (new, +1 test function/5 assertions): disabled is a true no-op (no directory ever created); enabling produces a snapshot carrying configuration; a snapshot older than `pitrRetentionHours`, seeded before the first real archive so one call exercises both "take" and "prune" without a sleep, is removed; a second call inside the configured interval takes no second snapshot. Full `npm test`: 9/9 suites green, exit 0.<br>- **A repeated environment-level interruption during this session's `npm run test:e2e` runs, unrelated to this or any other unit's code**: the full 43-test Playwright run was killed partway (exit 127, no error output) on three separate attempts, at different test counts each time (24, then 28) — never a real assertion failure, always a clean stop mid-stream. Rather than accept a partial signal, every test was run to completion by other means instead: `desktop-chromium` split across the two partial runs already covered `cashier-billing.spec.js` (6/6), `customer-portal.spec.js` (12/12) and `reprint-desk.spec.js` (6/6); `return-desk.spec.js` (7/7) and the `mobile-chromium` project (`customer-portal.spec.js` again, 12/12 at 390px) were then each run standalone to a clean exit 0. 6+12+6+7+12 = all 43 journeys, every one passing, none of them in a single unbroken invocation. Recorded plainly rather than claimed as one clean run, because it was not one. | - [backend/pitr.js](backend/pitr.js)<br>- [backend/defaultSettings.js](backend/defaultSettings.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_suite.js](backend/test_suite.js)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-21 | -- | AI Assistant | **Phase 41 (unit 2 of 6): the wastage pricing model — all three modes, flagged off** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5.1 — `[needs design decision: wastage model]` resolved by the owner as "build all three, selectable per tenant" rather than picking one, so nothing here waited on a single answer)<br>- **`013_invoice_line_wastage.sql` (new)** — three additive columns on `invoice_lines`: `wastage_mode` (CHECK IN `none`/`weight_uplift`/`making_charge_percent`/`separate_line`, default `none`), `wastage_weight_mg`, `wastage_amount_paise` (both `>= 0`, default `0`). Every row on disk today reads as `none`/`0`/`0` — exactly true of them.<br>- **`computeWastageAmount()` (new, `frontend/js/lib/billingMath.js`)** resolves a line's wastage to ₹ (and, for the weight-uplift model, extra grams) BEFORE it reaches `computeInvoiceTotals()` — that function only ever accepts already-priced ₹ figures, the same contract metal value and making charge already had. `weight_uplift` bills extra grams at the line's own rate; `making_charge_percent` and `separate_line` are IDENTICAL arithmetic (a percentage of the making charge) and differ only in how the invoice is meant to DISPLAY the charge, never in what it costs.<br>- **`computeInvoiceTotals()` gained an optional `wastageAmount`** (scalar and per-line), added into `preTaxTotal` at the same tier as making charge, before discount/tax — omitted or zero is BYTE-IDENTICAL to the function's behaviour before wastage existed (asserted directly in `test_billing_math.js` §19). `allocateLines()` carries it through so per-line figures still sum exactly to the header, and `netMetalValue` absorbs the residual the same way it already does for making charge. `saleLines()` and `computeReturnRefund()` both extended — a returned line now refunds its proportional share of whatever wastage was charged on it, priced off the ORIGINAL invoice exactly like metal and making charge already are.<br>- **WASTAGE IS A STORE-WIDE POLICY, NEVER A CLIENT-SUPPLIED FIGURE.** `backend/services/saleService.js#priceLine()` computes it from `settings.wastageEnabled`/`wastageMode`/`wastagePercent` for EVERY line, uniformly — the same posture the tax slab already has. A `test_http.js` check posts a sale that tries to smuggle its own `wastageMode`/`wastageAmount` in the request body and confirms both are ignored entirely in favour of the settings-configured model.<br>- **New settings**: `wastageEnabled` (boolean, default `false`), `wastageMode` (enum, default `weight_uplift`), `wastagePercent` (number, 0–25, default `0`) — through the existing `SETTINGS_FIELD_RULES` choke point. Settings UI: a new "Wastage" block under Tax & Invoice Numbering, folded into the existing Save Billing Settings action rather than a parallel save button.<br>- **Billing Desk**: `recalculateSummaryOnly()` now computes each line's wastage client-side with the SAME `computeWastageAmount()` call the server uses (same mode/percent/weight/rate inputs), so the on-screen preview matches what will actually be charged. A new summary row (`#summary-wastage-row`) appears only when wastage is enabled and the cart is non-empty — same "no row when there's nothing to say" convention as the existing discount/advance rows.<br>- **Migration numbering collided mid-build**: this session created `012_invoice_line_wastage.sql` before discovering a concurrent session had independently claimed `012_customer_master.sql` in the same uncommitted tree; renamed to `013` before either was committed — `npm run migrate:check-safety` clean at 13 files, no destructive DDL.<br>- **Verification.** `test_billing_math.js` §19 (+8: each mode's arithmetic, disabled-is-byte-identical, the generalised metal+making+wastage−discount=taxable identity across every slab and both tax modes, scalar-vs-lines[] equivalence, proportional refund on a wastage-charged line). `test_schema.js` (+3: default none/0/0, an unknown `wastage_mode` refused, negative weight/amount refused). `test_http.js` (+2: disabled prices identically to before; enabled charges uniformly from settings and ignores a tampered request). Full `npm test`: 153+68+135+16+... all 9 suites green, exit 0 (one transient exit 127 on an unrelated run, reproduced clean on retry — not this phase's code). `npm run test:e2e`: 43/43, run in full per CLAUDE.md §8 (this phase touches `BillingDesk.js` and `SettingsManager.js`, both forms). | - [backend/repositories/migrations/013_invoice_line_wastage.sql](backend/repositories/migrations/013_invoice_line_wastage.sql)<br>- [frontend/js/lib/billingMath.js](frontend/js/lib/billingMath.js)<br>- [backend/services/saleService.js](backend/services/saleService.js)<br>- [backend/repositories/invoiceRepository.js](backend/repositories/invoiceRepository.js)<br>- [backend/defaultSettings.js](backend/defaultSettings.js)<br>- [frontend/js/components/BillingDesk.js](frontend/js/components/BillingDesk.js)<br>- [frontend/js/components/SettingsManager.js](frontend/js/components/SettingsManager.js)<br>- [backend/test_billing_math.js](backend/test_billing_math.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_http.js](backend/test_http.js)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-21 | -- | AI Assistant | **Phase 43: customer master and accounting exports** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5.5, "Customer master with consent/preferences, deduplication, correction/export and legally appropriate deletion/anonymisation" plus the accounting-export half of "Paginated search/reports, accounting export, settlement/advance reconciliation, profitability and inventory ageing" — the settlement/profitability/ageing reports stay out of scope, per §5's own note not to build dashboard reports before their underlying reconciliation definitions are accepted)<br>- **What already existed, reused rather than duplicated.** `customers` (001_initial_schema.sql) was already the customer master — every walk-in gets a row via `ensureCustomerId()`, not just portal logins — and `GET /api/customer-accounts` already listed every one of them, not just login-holders. This phase is the admin-facing half that was missing: correction, marketing consent, duplicate detection, export, anonymisation. The existing "Customer Logins" tab/route was extended, not forked — a second tab doing the same phone/name/email lookup for different actions would be the "two ways to do the same thing" CLAUDE.md §1 rules out.<br>- **`012_customer_master.sql` (new) — three additive columns on `customers`**: `marketing_consent` (default 0 — DPDP-style affirmative opt-in, never inferred from the existing `notify_email`/`notify_push` transactional prefs), `consent_updated_at`, `is_anonymised`.<br>- **`customerRepository.js` gained an admin-facing half**, deliberately separate from `toAccount()`/`saveAccounts()` above it (the frozen shape `customerAuth.js` depends on, untouched): `listCustomersPaged()` (search by name/phone/email), `adminUpdateCustomer()` (correction; stamps `consent_updated_at` only when consent actually changes), `findPossibleDuplicates()` (same normalised name or email, different phone — **detection only**, never an automatic merge, because merging would mean rewriting `customer_id` on historical invoices/advances/credit notes, which this codebase treats as a financial record, not customer-master data), `anonymiseCustomer()` (scrubs name/email/login/reset-token columns and revokes every session; keeps phone, the FK/lookup key; never touches a ledger row — an invoice already carries its own `customer_name`/`customer_phone` snapshot from the moment of sale, which stays exactly as filed, both because a financial record is not rewritten after the fact and because standard tax-record retention law does not recognise a deletion request against it).<br>- **`invoiceRepository.js` gained `exportRows()`** — every invoice matching a date filter, capped at 20,000 rather than `search()`'s 200-row UI page, ordered chronologically ascending (a register reads oldest-first; the UI reads newest-first).<br>- **New routes**: `GET/PATCH /api/customers/:id`, `GET /api/customers/duplicates`, `POST /api/customers/:id/anonymise` (`requireApprover` — one-way, same gate as every other irreversible action in this tree), `GET /api/customers/export.csv` (customer master, data-portability export — never includes password hash/salt/reset-token columns), `GET /api/reports/sales-register.csv?from=&to=` (the accounting export: a plain CSV invoice register, reusing the existing `qrcode`-style "no new dependency" posture — one row per invoice, since this app tracks neither a per-line HSN split nor a customer's GSTIN/state, so it reports the single tax rate/amount the ledger actually holds rather than fabricating a CGST/SGST or B2B/B2C split it has no basis for). `GET /api/customer-accounts` now merges the master-record fields (id, consent, anonymised flag) into its existing response by phone, without touching `customerAuth.js`'s frozen account shape.<br>- **Consent changes and anonymisation are audited** (`CUSTOMER_CONSENT_GRANTED`/`WITHDRAWN`/`CUSTOMER_ANONYMISED`) — the two genuinely legally-significant actions; an ordinary name/email correction is not, to keep the trail from being flooded with routine edits.<br>- **`CustomerAccountsManager.js`** (nav label "Customer Logins" → "Customers"): a Consent column and badge, a Correct action (reused inline-edit-form pattern), an Anonymise action gated behind `canApprove()` client-side too (matching the server gate), a Check Duplicates panel, an Export CSV button, and an Accounting Export panel (from/to date pickers + Download Sales Register CSV) — housed in this tab for lack of a better home, since the sales register is not itself a customer-master concern.<br>- **Verification.** `test_schema.js` +3 (`marketing_consent`/`is_anonymised` CHECK constraints); `test_repositories.js` +8 (search by name/phone, correction preserves untouched fields, `consent_updated_at` stamped only on an actual consent change, a duplicate phone refused, name-collision duplicate detection, anonymisation scrubs PII/keeps phone/revokes sessions, unpaginated export) plus 2 for `exportRows()` (chronological ordering, a filter that excludes everything); full `npm test` 9/9 green throughout, both before and after the concurrent Phase 41 session's continued edits to the same shared files. A real local HTTP boot on an isolated port (5000 was held by an unrelated foreign process from a different project on this machine — left untouched, not killed, per the standing rule against touching another session's work): login → list (merged wire shape confirmed) → consent patch → duplicates → both CSV exports, all verified, then the smoke-test edit reverted to leave the tenant clean. A headless-Chromium run driving the real admin UI on that same isolated port: Consent column/badges render → Correct opens pre-filled → save updates the table and badge → Check Duplicates renders → both CSV exports trigger a real download event → the Anonymise button is present (not clicked, to avoid destroying data) confirming the `canApprove()` gate renders correctly for an owner login; zero console errors. | - [backend/repositories/migrations/012_customer_master.sql](backend/repositories/migrations/012_customer_master.sql)<br>- [backend/repositories/customerRepository.js](backend/repositories/customerRepository.js)<br>- [backend/repositories/invoiceRepository.js](backend/repositories/invoiceRepository.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_repositories.js](backend/test_repositories.js)<br>- [frontend/js/components/CustomerAccountsManager.js](frontend/js/components/CustomerAccountsManager.js)<br>- [frontend/index.html](frontend/index.html)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-20 | -- | AI Assistant | **Phase 40: invoice delivery status — closing the roadmap line** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5.3, the last piece of "split tenders, cash shifts/closing, quotes/holds, reprint and delivery")<br>- **`010_invoice_delivery.sql` (new) — four additive columns on `invoices`**, not a new table. Unlike lot inventory / cash shifts / sale drafts earlier this session, delivery is one more fact about an invoice that already exists, not a ledger of its own events — `delivery_status` (`pending`/`delivered`), `delivered_at`, `delivered_by_user_id`, `delivery_note`. Defaults every pre-existing invoice to `pending`, which is exactly true: nothing was ever marked delivered before this column existed.<br>- **Deliberately reversible — the one exception to this session's append-only pattern, and on purpose.** `markDelivered()`/`markPending()` (new, `invoiceRepository.js`) are plain `UPDATE`s with no immutability trigger, because operational status is not a financial fact the way `advance_entries`/`inventory_movements`/`cash_shifts` all are; a cashier who mis-taps needs to correct it the same route, not a second "undo" mechanism.<br>- **`toLegacySale()` gained three fields** (`deliveryStatus`/`deliveredAt`/`deliveryNote`) — the first change to this specific wire projection since the SQL cutover. CLAUDE.md calls the exact-field-list test in `test_repositories.js` load-bearing for catching an ACCIDENTALLY dropped field; extending it here for an INTENTIONALLY added one is the correct use of that same test, not a violation of what it guards.<br>- **`POST /api/sales/:invoiceNumber/delivery`** (`requireAdminSession`, body `{delivered, note?}`) and a **Delivery column on the Reprint Desk's results table** — Mark Delivered/Mark Pending toggle with a coloured status pill. No new tab: the Reprint Desk already looks an invoice up by number/phone/name, which is the same lookup delivery tracking needed, so reusing it kept this the smallest of this session's five Phase 5.3 pieces.<br>- **Closes `PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5.3's line entirely** — split tenders (2026-08-12), cash shifts (Phase 38), quotes/holds (Phase 39) and now delivery are all landed; only reprint predates this whole run (Phase 22).<br>- **Verification.** `test_schema.js` +1 (defaults to pending; an unknown `delivery_status` refused by the schema's own `CHECK`); `test_repositories.js` +4 (`markDelivered()` reaches the wire shape correctly, `markPending()` clears the facts rather than keeping them as history — a deliberate design choice, not an oversight — and a cross-tenant mark is refused); the field-list test updated and re-verified green; a headless-Chromium run — filed a real sale, found it on the Reprint Desk by customer name, clicked Mark Delivered (badge and button flipped), clicked Mark Pending (reverted cleanly), zero console errors throughout. Full 43/43 Playwright e2e re-run — `ReprintDesk.js` has its own dedicated spec (`reprint-desk.spec.js`), so this was not assumed safe by inspection. Tests: 514 → 519, green, exit 0.<br>- **Same concurrent-session situation as Phases 38–39** (see those rows) — the audit-retention work was still uncommitted and still touching the same shared files when this phase started; same surgical staging applied a third time. | - [backend/repositories/migrations/010_invoice_delivery.sql](backend/repositories/migrations/010_invoice_delivery.sql)<br>- [backend/repositories/invoiceRepository.js](backend/repositories/invoiceRepository.js)<br>- [backend/repositories/index.js](backend/repositories/index.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_repositories.js](backend/test_repositories.js)<br>- [frontend/js/components/ReprintDesk.js](frontend/js/components/ReprintDesk.js)<br>- [CLAUDE.md](CLAUDE.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-20 | -- | AI Assistant | **Phase 39: quotes and holds — save a cart without filing it** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5.3, the quotes/holds two-fifths of "split tenders, cash shifts/closing, quotes/holds, reprint and delivery")<br>- **`009_sale_drafts.sql` (new) — `sale_drafts`, the one table this session's phases added that is deliberately NOT append-only.** A quote is a price estimate never charged; a hold is a cart paused mid-transaction so the counter is free for the next customer. Neither commits money or stock, so neither gets the immutable treatment `advance_entries`/`inventory_movements`/`cash_shifts` all take — this is scratch state, fully mutable, `CHECK (json_valid(cart_json))` the only real guard. `backend/repositories/saleDraftRepository.js` (new): `createDraft`/`updateDraft`/`resumeDraft`/`discardDraft`/`listDrafts`, all working on an opaque cart blob this repository never prices or interprets.<br>- **The cart is priced by the Billing Desk, twice — once when saved, once when resumed — never by the backend.** This is what keeps the feature out of `computeInvoiceTotals`: a stale rate or a moved tax setting on a resumed quote is resolved exactly the way it always is for any sale, by the server's own recompute at `POST /api/sales`, not by this feature guessing at what "re-pricing a draft" should mean.<br>- **`GET`/`POST`/`PATCH /api/sale-drafts/*` plus `POST .../resume` and `.../discard`.** `requireAdminSession` only — the draft carries no money commitment, so it needs no approval gate. Two new buttons on the Billing Desk, `HOLD` and `QUOTE`, call `saveDraft(kind)` (new method, sends `activeLines()` — the cart exactly as displayed — plus customer/discount fields) and then `resetForm()`, same as after filing a real sale. A new `loadDraftCart()` method mirrors `resetForm()`'s field list in reverse to repopulate the cart on resume.<br>- **New Quotes & Holds tab** (`frontend/js/components/QuotesHoldsManager.js`) listing open drafts with Resume/Discard actions; Resume calls the backend transition, then calls `window.billingDesk.loadDraftCart()` directly and switches to the Billing Desk tab — the two components talk to each other through one small, deliberate public method rather than a shared event bus or global state.<br>- **Delivery is still not started** — this phase closes only the quotes/holds two-fifths of the roadmap line.<br>- **This is the first change in this session's phases to touch `BillingDesk.js`**, the largest and most heavily e2e-covered file in the tree — CLAUDE.md §8 calls a green `npm test` insufficient proof for anything touching a form, so the full 43-journey Playwright suite was re-run rather than assumed safe by inspection.<br>- **Verification.** `test_schema.js` +3 (kind/status enum CHECKs, `json_valid(cart_json)`, and an explicit assertion that an ordinary `UPDATE` on this table just works — no trigger blocks it, unlike every other table this session added); `test_repositories.js` +6 (an empty cart refused, a full create→edit→resume lifecycle, a resumed draft's terminal refusal to be edited or resumed again, `listDrafts()`'s default-to-open filtering); a headless-Chromium run against a real local boot — entered a 5g item and customer details on the Billing Desk, clicked HOLD, confirmed the app's own alert overlay and the reset form, found the hold under Quotes & Holds, clicked Resume, watched the Billing Desk reload with the exact customer/cart and compute a live total (₹38,946.88) through the completely unmodified pricing engine; full 43/43 Playwright e2e green, confirming no regression to the money-critical file. Tests: 505 → 514, green, exit 0.<br>- **Built inside the same concurrent-session situation as Phase 38** (see that row) — the audit-retention work was still uncommitted and still touching `server.js`/`repositories/index.js`/`test_schema.js`/`test_repositories.js` when this phase started. Same surgical staging applied again: reconstructed and verified a "HEAD + only this phase's lines" version of each shared file before staging, leaving the other session's additions exactly as found. | - [backend/repositories/migrations/009_sale_drafts.sql](backend/repositories/migrations/009_sale_drafts.sql)<br>- [backend/repositories/saleDraftRepository.js](backend/repositories/saleDraftRepository.js)<br>- [backend/repositories/index.js](backend/repositories/index.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_repositories.js](backend/test_repositories.js)<br>- [frontend/js/components/BillingDesk.js](frontend/js/components/BillingDesk.js)<br>- [frontend/js/components/QuotesHoldsManager.js](frontend/js/components/QuotesHoldsManager.js)<br>- [frontend/js/app.js](frontend/js/app.js)<br>- [frontend/index.html](frontend/index.html)<br>- [CLAUDE.md](CLAUDE.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-20 | -- | AI Assistant | **Phase 38: cash shifts — open with a float, close with a count** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5.3, the cash-shifts third of "split tenders, cash shifts/closing, quotes/holds, reprint and delivery")<br>- **`008_cash_shifts.sql` (new) — `cash_shifts`.** One open shift per branch at a time (a partial unique index, the same mechanism `advance_entries` uses for a duplicate reference). The opening facts (float, who opened it, when) are immutable from insert; once closed, the whole row is terminal — no reopening, no editing the count after the fact, enforced by two triggers rather than convention. `backend/repositories/cashShiftRepository.js` (new): `openShift`/`closeShift`/`getOpenShift`/`listShifts`, and `expectedCashAsOf()`, which is never a stored figure — always `SUM` over the shift's own time window, read live from `tenders` (`method='cash'`), `advance_entries` (`payment_method='cash', entry_type='deposit', status='posted'`) and `credit_notes` (`refund_mode='cash'`), the same "balance is derivable from history" posture the ledger already takes everywhere else. `closeShift()` freezes that computation once, at the moment of closing.<br>- **`GET`/`POST /api/cash-shifts/*`** (current shift + live preview, open, close, history) and a new **Cash Shifts tab** (`frontend/js/components/CashShiftManager.js`) showing the float/sales/deposits/refunds/expected breakdown live on an open shift, and the variance on every closed one. `requireAdminSession` only, no `requireApprover` — the shift record itself is the audit trail, the same call made for Phase 37's inventory adjustments; gating a large-variance close behind an approver is a reasonable follow-up, not built here.<br>- **Quotes/holds and delivery are still not started** — this phase closes only the cash-shifts third of the roadmap line.<br>- **A concurrent session was mid-edit on audit-log retention in this exact tree while this was built** (`backend/auditRetention.js`, `auditRetentionRepository.js`, migration `007`, plus edits to `defaultSettings.js`, `auditRepository.js`, and — sharing files with this phase — `server.js`, `repositories/index.js`, `test_schema.js`, `test_repositories.js`), all still uncommitted as this row is written. Confirmed with the user before proceeding rather than silently overwriting or silently including it. Where a shared file had both streams' changes in ONE git hunk with no separating context line (`test_schema.js`, `test_repositories.js`), a clean "HEAD + only this phase's lines" version was reconstructed and verified to run standalone (55 and 108 checks respectively, matching this phase's own additions exactly) before staging — so this commit contains only Phase 38, and the audit-retention session's work remains exactly as they left it, uncommitted and theirs to commit when ready. **The 505-check total this phase reports is this commit's own state; the actual working tree at the time of writing also contains the other session's additional, uncommitted checks on top.**<br>- **Verification.** `test_schema.js` +5 (one shift open per branch, opening facts immutable, the closed-terminal CHECK and trigger, a new shift after close); `test_repositories.js` +7 (a real cash sale via `saleService`, a real cash advance deposit via `advanceService`, and a real cash refund via `returnService` all correctly moving `expectedCashAsOf()`, the refused-second-open and refused-already-closed paths, `listShifts` ordering); a headless-Chromium run against a real local boot — opened a ₹5,000 float, confirmed the live preview, closed with an exact count, saw "Drawer matched exactly" in the app's own custom alert overlay, confirmed in history. Full 43/43 Playwright e2e re-run (another new nav button). Tests: 493 → 505, green, exit 0. | - [backend/repositories/migrations/008_cash_shifts.sql](backend/repositories/migrations/008_cash_shifts.sql)<br>- [backend/repositories/cashShiftRepository.js](backend/repositories/cashShiftRepository.js)<br>- [backend/repositories/index.js](backend/repositories/index.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_repositories.js](backend/test_repositories.js)<br>- [frontend/js/components/CashShiftManager.js](frontend/js/components/CashShiftManager.js)<br>- [frontend/js/app.js](frontend/js/app.js)<br>- [frontend/index.html](frontend/index.html)<br>- [CLAUDE.md](CLAUDE.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-20 | -- | AI Assistant | **Phase 37: lot inventory — items, lots, and immutable stock movements** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 5.2, the ungated slice)<br>- **Scope decided before writing any code.** The roadmap's own P2 section gates "vendor/purchase" and "branch transfer" behind a legal/business definition (GST reverse-charge treatment for buying from a vendor or a customer, inter-GSTIN accounting for moving stock between branches) that has never been made. Confirmed with the user before proceeding rather than guessing at business/tax rules: build the ungated half only — lot inventory and physical-count adjustments — and leave purchase receiving and branch transfer out entirely, not stubbed.<br>- **`006_lot_inventory.sql` (new) — `inventory_items`, `inventory_lots`, `inventory_movements`.** Items are mutable catalogue metadata (name/category/purity — `sku_code` reserved for the separate, not-yet-built barcode slice). A lot is a distinguishable batch of an item, opened by an `opening_balance` movement. `inventory_movements` is append-only by trigger, exactly matching `advance_entries`/`audit_events`: a lot's on-hand weight is `SUM(weight_delta_mg)` over its own movements, never stored. Weight-only (integer milligrams, matching `invoice_lines.weight_mg`) — no piece-count unit invented for a first slice that does not need one.<br>- **The negative-balance guard is app-level, not a trigger** (SQLite has no portable way to assert an aggregate from a row-level trigger) — `recordAdjustment()` computes the current balance inside the same transaction and refuses to post if the result would go negative, the same place `saleService` checks an advance redemption against its balance.<br>- **A LEFT JOIN bug caught before it shipped.** The first draft of `itemStockSummary()`'s branch filter lived in the `WHERE` clause, which silently turns a `LEFT JOIN` into an `INNER JOIN` — an item with zero lots in the filtered branch would have vanished from the report instead of showing at 0g. Moved into the `JOIN ... ON` clause instead; a new test (`test_repositories.js`) asserts the fixed behavior directly.<br>- **`backend/repositories/inventoryRepository.js` (new)** — `createItem`/`updateItem`/`listItems`/`getItem`, `openLot`/`getLot`/`listLots`/`lotBalanceMg`, `recordAdjustment`, `itemStockSummary`, `listMovements`. `recordAdjustment()` deliberately takes no `branchId` parameter — the movement's branch always comes from the lot it adjusts, never from whatever branch the caller currently claims to be in, so a future second branch cannot silently misattribute an adjustment.<br>- **`GET`/`POST /api/inventory/items`, `GET /api/inventory/stock`, `GET`/`POST /api/inventory/lots`, `POST /api/inventory/lots/:id/adjust`, `GET /api/inventory/movements`** — all `requireAdminSession` only, no `requireApprover`: the movement itself is the audit trail, matching this feature's straightforward-CRUD scope rather than `advance_entries`' approval workflow (noted as a natural follow-up if adjustment-based shrinkage ever needs gating). Body validation through the existing `validateBody()`/`validation.js` choke point, not a new one.<br>- **New Inventory tab** (`frontend/js/components/InventoryManager.js`) — item catalogue + stock table, "+ New Item"/"+ New Lot" inline forms matching `AdvancesManager.js`'s existing pattern, an expandable per-item lots view with an inline "Adjust" control, and a Recent Activity movement log. No purchase-receiving or branch-transfer action exists anywhere in the UI, matching the schema.<br>- **NOT wired into the sale/invoice flow.** A sale does not decrement stock — doing so would touch `computeInvoiceTotals`, which this roadmap's own "Cost note" on multi-line invoices calls the most-tested function in the tree, and deserves its own dedicated pass with its own `test_billing_math.js` coverage rather than riding in on this one.<br>- **Verification, in increasing order of realism.** `test_schema.js` +4 (the CHECK constraints via raw SQL, deliberately bypassing the repository's own JS guards, so the DB backstop is actually exercised); `test_repositories.js` +11 (full lot lifecycle, the transaction-required guard, the negative-balance refusal leaving the balance untouched, the LEFT JOIN fix); a real local HTTP boot against a fresh temp tenant with session cookies and CSRF — item → lot → +adjustment → refused negative adjustment (balance unchanged) → stock summary → movement history, plus the 401 boundary and a 400 on an invalid purity; a headless-Chromium run driving the actual admin UI end to end (login → new item → new lot → view lots → adjust → recent activity), screenshotted at three points and confirmed zero browser console errors. Tests: 478 → 493, green, exit 0. `npm run test:e2e` re-run in full (new nav button touches the shared sidebar every existing spec also renders against). | - [backend/repositories/migrations/006_lot_inventory.sql](backend/repositories/migrations/006_lot_inventory.sql)<br>- [backend/repositories/inventoryRepository.js](backend/repositories/inventoryRepository.js)<br>- [backend/repositories/index.js](backend/repositories/index.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_schema.js](backend/test_schema.js)<br>- [backend/test_repositories.js](backend/test_repositories.js)<br>- [frontend/js/components/InventoryManager.js](frontend/js/components/InventoryManager.js)<br>- [frontend/js/app.js](frontend/js/app.js)<br>- [frontend/index.html](frontend/index.html)<br>- [CLAUDE.md](CLAUDE.md)<br>- [docs/PRODUCTION_READINESS_ROADMAP.md](docs/PRODUCTION_READINESS_ROADMAP.md) |
| 2026-08-19 | -- | AI Assistant | **Phase 36: migration compatibility gate, canary/pilot rollout, and deploy-pipeline rollback** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 3 — "Migration compatibility gates, canary/pilot release, rollback and post-deploy synthetic checks")<br>- **Migration compatibility gate.** `backend/repositories/migrate.js#checkMigrationSafety()` (`npm run migrate:check-safety`) statically scans every migration on disk, comment-stripped, for `DROP TABLE`/`DROP COLUMN`/`RENAME COLUMN`/`RENAME TO` and fails loudly naming the file — CLAUDE.md §1 already requires additive, backward-compatible schema changes, and nothing enforced it beyond code review until now. Deliberately does NOT flag `DROP INDEX`/`DROP TRIGGER`/`DROP VIEW` (003 already drops and replaces a unique index; that loses no data). No database involved, so it runs in CI with nothing installed but Node — new `migration-safety` job in `daily-checks.yml`.<br>- **Canary/pilot rollout.** A release manifest published to `licensing_server` gains `rolloutPercent` (1-100, default 100, part of the signed payload so a POS client can trust it). `backend/updateEngine.js#isInRolloutCohort()` hashes `licenseKey:version` (SHA-256, first 4 bytes mod 100) so cohort membership is deterministic per tenant per version — stable across daily checks, and widening a rollout only ever ADDS tenants, never drops one already auto-applying. `GET /api/releases/latest`'s picking logic now prefers the most-recently-published entry on a version tie, which is what makes "republish the same version at a higher percentage" actually take effect. Only the security channel auto-applies, so only it is gated; a held-back tenant just gets re-evaluated next daily check (`UPDATE_ROLLOUT_HELD_BACK` telemetry), same low-drama posture as the existing "licensing server unreachable" holdback. Admin dashboard gained a Rollout % input on the publish form.<br>- **Deploy-pipeline rollback.** `deploy/remote-deploy.sh` now writes `.rollback-sha` at the checkout path (the commit it was about to move off of) before every normal deploy, and accepts a trailing `--rollback` flag that resets to exactly that commit instead of the branch tip. All three `cd-*.yml` workflows now run the smoke test with `continue-on-error`, call `--rollback` over SSH when it fails, re-poll `/api/ready`, and **still fail the job either way** — a rollback restores service, it does not make a bad build good. Scoped to the backend deploy only; the conditional licensing-server sub-deploy has no smoke test today and was left alone.<br>- **Post-deploy synthetic checks**: found already substantially covered — `GET /api/ready` (what every `cd-*.yml` smoke test polls) runs a real query and confirms zero pending/drifted migrations, not just process-alive. Left as-is rather than risk writing synthetic test data into a live tenant's ledger to probe deeper.<br>- **Verification.** `backend/test_schema.js` 43→46 (safety gate against fixture strings, comment-stripping, and the real on-disk migrations — currently clean); `backend/test_suite.js` Test 12 (cohort determinism, monotonic widening, per-tenant independence across 50 synthetic license keys). Manually verified against a real local `licensing_server` boot: published a release at 10% rollout, confirmed `rolloutPercent` round-trips through the decoded signed payload, republished the same version at 100%, confirmed the widened entry wins on the tie, confirmed a `rolloutPercent: 150` publish is rejected. The rollback shell logic was exercised end-to-end against a throwaway local git repo (deploy A→B records `.rollback-sha` = A; `--rollback` correctly resets to A) with a stubbed `pm2`. **Not verified: the SSH/GitHub-Environment wiring itself** — same caveat as Phase 34's CI security-scanning jobs, this needs a real pull request and, for the deploy workflows specifically, a real VPS; neither exists yet (§7). Tests: 474 → 478 (test_schema +3, test_suite gains Test 12), green, exit 0. | - [backend/repositories/migrate.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/repositories/migrate.js)<br>- [backend/test_schema.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_schema.js)<br>- [backend/test_suite.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/test_suite.js)<br>- [backend/updateEngine.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/updateEngine.js)<br>- [backend/package.json](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/backend/package.json)<br>- [licensing_server/server.js](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/licensing_server/server.js)<br>- [deploy/remote-deploy.sh](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/deploy/remote-deploy.sh)<br>- [deploy/README.md](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/deploy/README.md)<br>- [.github/workflows/daily-checks.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/daily-checks.yml)<br>- [.github/workflows/cd-dev.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-dev.yml)<br>- [.github/workflows/cd-sandbox.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-sandbox.yml)<br>- [.github/workflows/cd-live.yml](file:///c:/Users/ABCD/Documents/Antigravity%20Projects/Web%20POS/.github/workflows/cd-live.yml) |
| 2026-08-19 | -- | AI Assistant | **Phase 35: operational alerting** (`PRODUCTION_READINESS_ROADMAP.md` §5 Phase 3 — "Alert on payment/webhook failures, ledger imbalance, backup failure, stale rates, error/latency, capacity, TLS expiry and control-plane failure")<br>- **`backend/alerting.js` (new) — one choke point, `raiseAlert()`.** Every alert always writes to `error.log`/`telemetry.log` (`logError`/`logTelemetry`), so nothing is lost with no SMTP configured; email is a best-effort add-on through the same `sendMailIfConfigured()` transport `emailReporter.js` already uses — no new dependency, no second mailer. A **per-code, not global, 30-minute cooldown** stops a misconfiguration that fires on every request (an unset webhook secret) from sending one email per request, while every occurrence still lands in the log files.<br>- **Every check returns the alert codes it raised** (`[]` when clean) instead of void, so a cron tick can ignore the value and a test can assert on it directly rather than scraping log output.<br>- **Payment/webhook failures**: hooked at the existing `logError` call sites in the Razorpay webhook route (`server.js`) and `paymentService.js#creditCapturedPayment()` — signature mismatch, unknown order, malformed entity, amount mismatch, and the two "money taken but not credited" reconciliation paths.<br>- **Ledger imbalance**: new `invoiceRepository.js#findLineDrift()` (the per-line-sums-to-header invariant CLAUDE.md §0 requires, checked against what's actually on disk) plus the existing `audit.verifyChain()` and `isDataStoreReady()`, run daily via `checkLedgerIntegrity()`.<br>- **Backup failure**: `backupEngine.js#createBackup()` now alerts on its own failure, and — since a file that copied is not proof it can restore a shop — **spawns `verifyBackup.js` after every successful nightly backup** and alerts if restore verification fails, carrying the PASS/FAIL report in the alert body. A separate daily `checkBackupFreshness()` catches the case the spawn can't: the cron not firing at all. Both `backupEngine.js` and `alerting.js` gained a `GOLD_POS_BACKUPS_DIR` override (same convention as `DATA_DIR`/`LOGS_DIR`) so this is testable without reading the tenant's real backup history.<br>- **Stale rates, disk capacity, TLS expiry, control-plane**: `checkStaleRates()` (rate sync age vs. threshold), `checkDiskCapacity()` (`fs.statfsSync` on `DATA_DIR` — stdlib, no dependency), `checkTlsExpiry()` (no-ops unless `settings.publicUrl` is `https://`, matching the emailReporter "degrade, don't crash" contract — the roadmap notes no VPS/domain is provisioned yet, so this is currently dormant by design), and `CONTROL_PLANE_UNREACHABLE`/`RELEASE_SIGNATURE_INVALID`/`UPDATE_APPLY_FAILED` alerts added to `licenseChecker.js` and `updateEngine.js`'s existing licensing-server catch blocks.<br>- **HTTP error-rate / p95 latency**: `recordRequestOutcome()` hooked into the request-telemetry middleware already in `server.js` (the one place every response passes through); a 5-minute cron tick evaluates a 10% 5xx-rate threshold and a 3s p95 threshold against a minimum 20-request sample, then resets the window.<br>- **New setting `alertEmail`** (`defaultSettings.js`, falls back to `reportEmail`) with a Settings UI field, so ops alerts can go to a different inbox than the daily business summary.<br>- **`backend/test_alerting.js` (new, 16 checks)** — the cooldown behaviour, every check function's clean/dirty paths (including a raw-SQL-corrupted invoice line to prove `LEDGER_LINE_DRIFT` fires, the same tamper technique `test_schema.js`/`verifyBackup.js` use), and the error-rate/latency window's threshold math. Wired into `npm test` as suite 9.<br>- **Tests: 458 → 474, green, exit 0.** `npm run test:e2e` not re-run (`alertEmail` is a same-shaped sibling of the already-covered `reportEmail` field, not a new auth/body path — CLAUDE.md §8's e2e trigger). Instead verified against a real local boot: started `node server.js` against the actual tenant `backend/data/`, confirmed the alert scheduler initialised, `CONTROL_PLANE_UNREACHABLE` fired for real (no licensing server running locally) and landed in `error.log`/`telemetry.log`, the served `SettingsManager.js` bundle contains the new field's three wire-up points, `/api/settings` still 401s without a session, and the nightly `createBackup()` → `verifyLatestBackupAsync()` spawn produced a real `BACKUP_VERIFY_SUCCESS` telemetry line against the tenant's actual latest snapshot. | - [backend/alerting.js](backend/alerting.js)<br>- [backend/test_alerting.js](backend/test_alerting.js)<br>- [backend/backupEngine.js](backend/backupEngine.js)<br>- [backend/priceEngine.js](backend/priceEngine.js)<br>- [backend/licenseChecker.js](backend/licenseChecker.js)<br>- [backend/updateEngine.js](backend/updateEngine.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/services/paymentService.js](backend/services/paymentService.js)<br>- [backend/repositories/invoiceRepository.js](backend/repositories/invoiceRepository.js)<br>- [backend/defaultSettings.js](backend/defaultSettings.js)<br>- [frontend/js/components/SettingsManager.js](frontend/js/components/SettingsManager.js) |
| 2026-08-24 | -- | AI Assistant | **Security audit remediation: C1–C5, H1–H8** (see `docs/SECURITY_AUDIT.md`, dated 2026-08-21). The audit's #1 finding was architectural: `POST /api/settings` was gated only by `requireAdminSession`, so any signed-in cashier could rewrite the whole settings document — including adding themselves as `owner` — in one request.<br>- **C1** — new `requireRole(...roles)` middleware in `backend/adminAuth.js`, layered after `requireAdminSession`; `POST /api/settings` is now owner-only, and `mergeOperators()` independently refuses any incoming `owner`-role row from a non-owner caller (defence-in-depth for a future route that might reuse it).<br>- **C2** — `licensing_server/server.js` now refuses to boot under `NODE_ENV`/`ENV_NAME=production` while `ADMIN_SECRET` is still the documented default (mirrors `backend/productionGuard.js`'s fail-closed posture); every `POST /api/admin/releases` publish is now appended to `licensing_server/data/admin_audit.log` (who/what/when/IP); the admin secret is no longer printed to stdout on boot.<br>- **C3** — `requireRole` applied to `GET/POST /api/customer-accounts*` (owner/manager), `GET /api/diagnostics/*` and `POST /api/admin/update/*` (owner-only), and `POST /api/backup/run`, `/api/reports/send-now`, `/api/gold-price/sync` (owner/manager) — all previously reachable by any cashier session.<br>- **C4** — `frontend/index.html`'s `window.alert` override interpolated the message straight into `innerHTML`; rewritten to build the dialog from DOM nodes with `textContent`, mirroring the pattern `customer.html` already used.<br>- **C5** — every inline `<script>` block and `onclick=""` attribute in `index.html` and `customer.html` moved into `frontend/js/` (new: `adminAlertOverride.js`, `customerAlertOverride.js`, `customer-app.js`, the ~870-line module `customer.html` had inline); CSP's `scriptSrc` no longer carries `'unsafe-inline'` (styleSrc keeps it — inline `style=""` is pervasive by design and isn't an XSS vector the way inline script is).<br>- **H1** — minimum PIN length raised 4→6 digits for any PIN being *set* via `POST /api/settings` (existing shorter PINs on disk still verify at login); added a tenant-wide failed-admin-login breaker in `adminAuth.js` (100 failures/15min, independent of source IP, → 5min global lockout) alongside the existing per-IP escalating lockout — true per-operator counting isn't possible here since a wrong PIN has no username to attribute it to.<br>- **H2/H3** — `POST /api/payment/verify` rate-limited per customer (60/hr); `GET /api/qrcode` rate-limited (60/min) and `data` capped at 200 chars; `POST /api/license/activate` rate-limited (20/hr).<br>- **H4** — `POST /api/customer/register`'s `ACCOUNT_EXISTS`/`CLAIM_REQUIRES_STORE` responses (which let anyone probe whether a phone number was a known customer) merged into one `REGISTRATION_BLOCKED` response with the same wording either way.<br>- **H5** — stripped `err.message` from the client response on the 5 audit-confirmed leak sites (`/api/payment/order`, `/api/sales`, `/api/license/activate`, `/api/gold-price/sync`, plus `/api/payment/verify`), replaced with a generic message + `requestId`; `logError()` still captures the full detail server-side.<br>- **H6/H7/H8** — `licensing_server`: `/api/license/verify` rate-limited (30/hr/IP); `failedAdminAttempts` swapped from a plain `Map` to a bounded, TTL-swept one (same shape as `backend/rateLimit.js`'s `createBoundedMap`, duplicated rather than imported — separate deployable, separate dependency budget); `cors()` removed outright (the `cors` package dropped from `package.json`/`package-lock.json` — nothing here needs cross-origin access), replaced with a handful of manual security headers; a blanket per-IP rate limiter added ahead of every route.<br>- **Not addressed this pass** (see `docs/SECURITY_AUDIT.md` for the reasoning): M1 (extreme-discount alerting, needs a product/threshold decision), L1–L3 (deployment-time hardening, not code).<br>- **Tests: 606 → 611 (5 new HTTP-boundary checks pinning C1/C3/H3/H4), green, exit 0. `npm run test:e2e` re-run in full given the `index.html`/`customer.html` restructuring — 43/43 green.** `licensing_server` has no automated suite (pre-existing gap, not built in this pass); verified manually — boots in dev with a warning, refuses to boot in production with the default secret (`exit 1`), security headers present, CORS header absent, `/api/license/verify` 429s on the 30th request within its window. | - [backend/adminAuth.js](backend/adminAuth.js)<br>- [backend/server.js](backend/server.js)<br>- [backend/test_http.js](backend/test_http.js)<br>- [licensing_server/server.js](licensing_server/server.js)<br>- [licensing_server/package.json](licensing_server/package.json)<br>- [frontend/index.html](frontend/index.html)<br>- [frontend/customer.html](frontend/customer.html)<br>- [frontend/js/adminAlertOverride.js](frontend/js/adminAlertOverride.js)<br>- [frontend/js/customerAlertOverride.js](frontend/js/customerAlertOverride.js)<br>- [frontend/js/customer-app.js](frontend/js/customer-app.js)<br>- [frontend/js/components/SettingsManager.js](frontend/js/components/SettingsManager.js)<br>- [docs/SECURITY_AUDIT.md](docs/SECURITY_AUDIT.md) |
| 2026-08-31 | -- | AI Assistant | **Security audit follow-up: L2 made locally provable (deploy/verify-nginx-proxy.sh)** (see `docs/SECURITY_AUDIT.md` L2, open since 2026-08-21).<br>- L2 says the licensing server's Nginx exposure is "not proven end-to-end" and stays open because there's no VPS/domain to prove it against (CLAUDE.md §7). Nothing about that changes — DNS resolution, a real Let's Encrypt cert, and `ufw` + the public internet reaching the box are all inherently infra, not code, and none of them are testable on a laptop.<br>- What *is* code is `deploy/nginx.conf.template`'s `proxy_pass`/header directives themselves — whether they actually forward a request to the loopback-bound app correctly. New `deploy/verify-nginx-proxy.sh` proves exactly that half: boots `licensing_server/server.js` on an ephemeral port (16060, not 6060 — never collides with a real local dev boot) with a throwaway `ADMIN_SECRET`, renders the same template `deploy/provision-pipeline.sh` uses for a test hostname, runs it in an `nginx:alpine` Docker container, curls through it, and tears both down.<br>- **Git Bash + Docker Desktop path-mangling caveat, documented inline in the script:** Git Bash rewrites any leading-`/` argument to a Windows path before exec'ing `docker.exe`, which corrupts the *container-side* half of a `-v host:container` mount unless `MSYS_NO_PATHCONV=1` is set and the host-side path is pre-converted with `cygpath -w`. Left as a comment at the exact line so a future edit doesn't silently reintroduce the bug.<br>- **Verification: ran it for real**, not just wrote it — Docker Desktop wasn't running, started it, waited for the daemon, then a live run passed: `{"status":"ok","version":"1.0.0","env":"production"}` returned through the proxy, `nginx -t` clean, and both the container and the `node` process confirmed gone afterward (empty `docker ps -a` for the container name, `:16060` unreachable).<br>- Docs updated to match: `docs/SECURITY_AUDIT.md` (new 2026-08-31 follow-up note under the pass log, plus a dated addendum in L2's own section), `deploy/README.md` §4 (new paragraph pointing at the script), `docs/GO_LIVE_CHECKLIST.md` A7 (new optional pre-flight bullet). L2 itself stays **OPEN** in the findings table — this closes nothing, it only shrinks what the eventual VPS session still has to debug blind. | - [deploy/verify-nginx-proxy.sh](deploy/verify-nginx-proxy.sh)<br>- [deploy/README.md](deploy/README.md)<br>- [docs/SECURITY_AUDIT.md](docs/SECURITY_AUDIT.md)<br>- [docs/GO_LIVE_CHECKLIST.md](docs/GO_LIVE_CHECKLIST.md) |
| 2026-09-01 | -- | AI Assistant | **Fixed two `npm test` failures on `9ca2b0e`, both pre-existing in the Phase 44-cont. work and unrelated to each other.** Found while preparing to merge this branch into `main` ahead of first production provisioning (`docs/GO_LIVE_CHECKLIST.md` Track A) — `main` cannot run code that doesn't pass its own test gate.<br>- **`test_http.js` "same-day void restores linked stock and all four management reports answer over HTTP"** was hitting `GET /api/reports/*` without enabling `managementReportsEnabled` — an earlier, unrelated check ("enabling management reports makes them reachable") correctly resets that flag to `false` in its own `finally` as cleanup, and this later test never re-enabled it before relying on the routes being reachable. Not a route bug: fixed by wrapping this test's report checks in the same enable/`finally`-restore pattern its sibling test already uses.<br>- **`test_suite.js` Test 15 "whole-archive backup encryption"** — `verifyBackup.js`'s "the books are actually there" check (`invoices > 0 \|\| advanceEntries > 0`) was failing because `createBackup()` was snapshotting a completely empty ledger: `test_suite.js` is pure crypto/licensing/account-machinery and never wrote a single invoice or advance entry anywhere in the file, so the restore drill had nothing real to find. Fixed by seeding one fixture deposit via `advanceService.recordDeposit()` (the same service-layer seam every other suite uses for this, never a raw insert) immediately before `createBackup()` runs.<br>- **Bonus fix, found while debugging the above**: `verifyBackup.js --quiet` suppressed its own failure summary along with its normal PASS/FAIL log lines — meaning the one time this tool's output matters most (a failed monthly restore drill, or exactly this debugging session), `--quiet` hid it. `record()`'s pass/fail lines stay gated on `!quiet`, but the failure summary at the bottom now always prints via `console.error`, regardless of `--quiet` — quiet mode is for keeping a clean cron log short, not for hiding an actionable failure.<br>- **Verification:** `cd backend && npm test` — 618 checks, green, exit 0, all nine suites. | - [backend/test_http.js](backend/test_http.js)<br>- [backend/test_suite.js](backend/test_suite.js)<br>- [backend/verifyBackup.js](backend/verifyBackup.js) |
| 2026-09-01 | -- | AI Assistant | **First real provisioning run: `docs/GO_LIVE_CHECKLIST.md` Track A5 executed against a live DigitalOcean VPS (`luminapos.in`, `--profile minimal`) — closes security-audit finding L2 for real, and found+fixed a genuine deploy-tooling bug no local test could catch.**<br>- **L2 (`docs/SECURITY_AUDIT.md`) closed.** `deploy/provision-pipeline.sh` ran end-to-end on a fresh 1GB Ubuntu 24.04 droplet: Node 24/PM2/Nginx/Certbot/ufw installed, both checkouts cloned, real Let's Encrypt certificates issued for `app.luminapos.in` and `license.luminapos.in`. `curl https://license.luminapos.in/api/health` from outside the VPS returned a real, healthy response — real DNS, real TLS, real reverse proxy, real firewall, no shortcuts. `app.luminapos.in` correctly answered a clean Nginx `502` (not a stack trace), matching its intentionally-not-yet-configured state (Track C — real Razorpay/PIN/rate-provider credentials, not this session's scope).<br>- **Found and fixed: `deploy/ecosystem.base.cjs`'s `cwd` didn't match where `provision-pipeline.sh` writes each app's `.env`.** Both `backendApp()` and `licensingApp()` set PM2's working directory to the checkout root, but the script writes `.env` into the module subdirectory (`backend/.env`, `licensing_server/.env`). Both entry points load config via `import 'dotenv/config'`, which defaults to reading `.env` from `process.cwd()` — so on the real VPS it silently found nothing on either app. The practical effect: `licensing-live` kept running the placeholder `ADMIN_SECRET` instead of the freshly generated one the script had actually written to disk — and correctly refused to start, which is exactly what security-audit finding C2's fail-closed check exists to catch, so nothing was ever exposed. Fixed at the one choke point both apps share: each `env` block in `ecosystem.base.cjs` now sets `DOTENV_CONFIG_PATH` explicitly (dotenv already supports this via `lib/env-options.js`), so `.env` loading no longer depends on the process's working directory at all. Verified live: patched both checkouts on the VPS, restarted, `licensing-live` came up `online` and `curl 127.0.0.1:6060/api/health` answered clean.<br>- **Found and fixed: a PowerShell quoting bug in both locally-generated SSH keys.** `ssh-keygen ... -N '""'` in PowerShell passes the *literal two-character string* `""` as the passphrase, not an empty one — so `luminapos_admin` and `gold_pos_ci` were both encrypted with an unintended passphrase, making them silently unusable for unattended/CI SSH (`Permission denied (publickey)` with no clue why, since the public half matched perfectly). Diagnosed by deriving the public key from the private file and comparing fingerprints, then confirming `ssh-keygen -y -P '""' -f <key>` succeeded. Fixed in place with `ssh-keygen -p -P '""' -N "" -f <key>` — no need to regenerate or re-distribute the public half.<br>- **Fixed two pre-existing `npm test` failures** (unrelated to the above, found while verifying the tree before merging to `main` ahead of this provisioning run): `test_http.js`'s management-reports check wasn't re-enabling a setting an earlier, unrelated test had reset in its own cleanup; `test_suite.js`'s backup-encryption restore drill was snapshotting a completely empty ledger (that suite never writes an invoice or advance entry anywhere) so `verifyBackup.js`'s "the books are actually there" check legitimately failed every run — now seeds one fixture deposit first. Also: `verifyBackup.js --quiet` was suppressing its own failure summary, exactly backwards for a restore-drill tool.<br>- **`main` updated and pushed** (commit `b7b50b0`, fast-forward from `aaa4af3`) ahead of this run, carrying the above test fixes plus the 2026-08-30/31 security-audit follow-up (L1, L3 closed; `deploy/verify-nginx-proxy.sh`) that had been sitting uncommitted. `cd backend && npm test` — 618 checks, green, exit 0.<br>- **A6 also done same day, via `gh` CLI** (repo is public, already authenticated as owner): `VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY` secrets, `PIPELINE_DOMAIN` variable, and the `production` environment with `Vishalnayak226` as sole required reviewer — confirmed via `gh secret list`/`gh variable list` and the environment API response.<br>- **Still open**: A7's CI-driven verification (push to `main`, watch `cd-live.yml` pause for approval — infra half already proven manually above), A8/Track C (real Razorpay/PIN/rate-provider config to bring `app.luminapos.in` actually up), off-site backup real remote mount + recovery drill, all merchant/legal/CA sign-off items. | - [deploy/ecosystem.base.cjs](deploy/ecosystem.base.cjs)<br>- [docs/SECURITY_AUDIT.md](docs/SECURITY_AUDIT.md)<br>- [docs/GO_LIVE_CHECKLIST.md](docs/GO_LIVE_CHECKLIST.md)<br>- [backend/test_http.js](backend/test_http.js)<br>- [backend/test_suite.js](backend/test_suite.js)<br>- [backend/verifyBackup.js](backend/verifyBackup.js) |
| 2026-09-02 | -- | AI Assistant | **`backend/licenseChecker.js` resolved its public-key path from `process.cwd()` instead of `__dirname` — a third instance, same bug class as the Sep 1 `ecosystem.base.cjs` fix — found while actually activating a license against the live VPS.** `PUBLIC_KEY_FILE = path.join(process.cwd(), 'keys', 'license_public.pem')` resolves correctly only when a process happens to be launched with its cwd already equal to `backend/` — true for every local dev/test invocation, false under PM2 (cwd is the checkout root), so no test ever could have caught it. Every sibling file that reads a key from disk (`cryptoHelper.js`, `blackBoxLogger.js`, `updateEngine.js`) already used the correct `path.join(__dirname, 'keys', ...)` pattern, and `licenseChecker.js` already had `__dirname` defined two lines above the bug — this was a one-line inconsistency, not a missing mechanism. Fixed to match.<br>- **A second, unrelated problem surfaced and self-inflicted while chasing the first: a stale git-tracked placeholder key got reintroduced onto the live VPS.** `licensing_server/keys/license_public.pem` is a tracked file the running server overwrites with its own real, self-generated public key on first boot (`deploy/provision-pipeline.sh`'s own "Signing keys" section documents this exact trap and the fix — restore from `KEY_OVERLAY_DIR`, which `deploy/remote-deploy.sh` does automatically after every deploy). The 2026-09-01 session's VPS-sync used a raw `git reset --hard` instead of `remote-deploy.sh`, which skipped that restoration step and silently reverted the licensing server's public key file back to the git-tracked dev-laptop placeholder — while its private key, gitignored, was untouched. The two calls this produced looked identical from the outside for a while: first "key file missing" (the `licenseChecker.js` bug above), then — after fixing that — "cryptographic signature mismatch," because the public key being checked against genuinely no longer matched the key actually signing tokens. Diagnosed by deriving the public key straight from the private key file (`openssl rsa -in ... -pubout`) and comparing checksums against every copy on disk, rather than assuming any one file was the source of truth. Fixed by rebuilding both `license_public.pem` and `release_public.pem` from their private keys and re-syncing every copy (`licensing_server/keys/`, `backend/keys/`, both `KEY_OVERLAY_DIR` entries) — verified end to end: `POST /api/license/activate` with `DEMO-KEY-12345` against `https://app.luminapos.in` now returns `"success":true`.<br>- **Operational lesson, not a code gap**: the existing `deploy/README.md` "Signing keys" section already documented this exact trap correctly — the mistake was a manual `git reset --hard` bypassing the tool that was built specifically to avoid it. Any future sync of a licensing checkout on this VPS should go through `deploy/remote-deploy.sh`, not a raw `git reset --hard`.<br>- **Verification:** `cd backend && npm test` — green, exit 0, all nine suites (unaffected — no test exercises this cwd-dependent path, by construction, since local test cwd always equals `backend/`). | - [backend/licenseChecker.js](backend/licenseChecker.js) |
