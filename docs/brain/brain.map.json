{
  "$comment": "The only hand-edited brain file. Regions claim files by glob, so a new file in an existing area usually needs no edit at all. Redraw with: node docs/brain/build-brain.mjs",
  "project": "Gold POS",
  "lobes": [
    {
      "id": "pos",
      "name": "The POS Terminal",
      "blurb": "What the shop actually runs: the Express API, the JSON store, pricing math, and the cashier UI."
    },
    {
      "id": "customer",
      "name": "The Customer Surface",
      "blurb": "What a customer touches: the mobile portal, its authentication, and payment collection."
    },
    {
      "id": "platform",
      "name": "The SaaS Platform",
      "blurb": "Licensing, signed updates, cryptography, and the tenant extension surface. Spans both servers."
    },
    {
      "id": "ops",
      "name": "Build & Operations",
      "blurb": "Packaging, deployment, CI, tests, and the local dev loop."
    },
    {
      "id": "knowledge",
      "name": "Docs & Agent Config",
      "blurb": "The trackers, the reference docs, and the rules an agent session loads."
    }
  ],
  "regions": [
    {
      "id": "pos-api",
      "lobe": "pos",
      "name": "POS API Router",
      "role": "The single Express router for the shop terminal — sales, payments, analytics, settings, and every /api route the cashier UI calls. The choke point most cross-cutting fixes belong at.",
      "match": [
        "backend/server.js"
      ]
    },
    {
      "id": "persistence",
      "lobe": "pos",
      "name": "Persistence & Settings",
      "role": "The SQLite datastore and the JSON layer it replaces. backend/repositories/ is the ADR-001 seam: connection/PRAGMAs, the migration runner, the numbered migrations, and one repository per domain — no SQL string exists above this directory, which is what keeps the documented move to PostgreSQL a swap rather than a rewrite. importLegacyJson.js carries a live tenant across from the JSON ledger with a dry run, a reconciliation report and a rollback. db.js/defaultSettings.js are the legacy JSON writers plus the settings default-merge-and-retire mechanism; settings and licence stay JSON on purpose. seed.js generates the deterministic synthetic fixture database.",
      "match": [
        "backend/db.js",
        "backend/defaultSettings.js",
        "backend/seed.js",
        "backend/repositories/**",
        "backend/importLegacyJson.js"
      ]
    },
    {
      "id": "domain-services",
      "lobe": "pos",
      "name": "Domain Services",
      "role": "The money rules, sitting between the routes and the repositories. A sale is one ACID transaction here — number allocation, header, lines, tenders, advance redemption and audit commit together or not at all — and the advance-balance check runs inside it, which is what stops two tills spending one balance. Services own the pricing decision and the persistence; routes own only HTTP parsing and status codes.",
      "match": [
        "backend/services/**"
      ]
    },
    {
      "id": "pricing",
      "lobe": "pos",
      "name": "Pricing & Billing Math",
      "role": "Gold rate sync, rate overrides, and the DOM-free billing helpers shared by the browser and the Node test suite. Making charges, GST, bi-directional rounding, the per-line allocation that makes a multi-line invoice's rows sum to its total, the saleLines() seam that reads a stored sale of either shape, and the return-refund pipeline all live here. Wastage does NOT — it exists nowhere in the tree; see the roadmap Phase 5 note.",
      "match": [
        "backend/priceEngine.js",
        "frontend/js/lib/*.js"
      ]
    },
    {
      "id": "pos-ui",
      "lobe": "pos",
      "name": "Cashier UI Shell",
      "role": "The admin single-page terminal: boot sequence, license gate, navigation controller, and the whole visual vocabulary. Vanilla JS/CSS served straight off disk.",
      "match": [
        "frontend/index.html",
        "frontend/js/app.js",
        "frontend/css/*.css"
      ]
    },
    {
      "id": "pos-components",
      "lobe": "pos",
      "name": "Cashier UI Components",
      "role": "The screens themselves — billing desk, dashboard, advances ledger, settings. The pattern any new screen should copy rather than reinvent.",
      "match": [
        "frontend/js/components/*.js"
      ]
    },
    {
      "id": "customer-portal",
      "lobe": "customer",
      "name": "Customer Portal",
      "role": "The customer-facing mobile page, its password-gated session flow, and offline UPI QR generation. Session-scoped as of Phase 20.1 — previously-public endpoints are now gated.",
      "match": [
        "frontend/customer.html",
        "backend/customerAuth.js",
        "frontend/js/qrGenerator.js"
      ]
    },
    {
      "id": "licensing",
      "lobe": "platform",
      "name": "Licensing",
      "role": "The RSA handshake that decides whether a tenant may run: the central signing service on :6060, the client-side signature verification, and the 7-day grace window.",
      "match": [
        "backend/licenseChecker.js",
        "licensing_server/**"
      ]
    },
    {
      "id": "updates",
      "lobe": "platform",
      "name": "Release & Update Engine",
      "role": "Packaging a release, signing it, and the tiered auto/manual apply-and-rollback path on the tenant side. Security-channel releases apply themselves; feature and patch ones ask.",
      "match": [
        "backend/updateEngine.js",
        "release_pipeline.js",
        "CHANGELOG.md"
      ]
    },
    {
      "id": "security",
      "lobe": "platform",
      "name": "Crypto, Auth & Black Box",
      "role": "Admin identity and every credential the terminal verifies. adminAuth.js owns the lot: scrypt-hashed PINs (one tenant-wide authSalt, because a PIN-only login has no username to look a per-user salt up by), the migration that converts a tenant's plaintext PINs on boot and deletes them, the named-operator roster and its four roles, session issue/expiry/revocation, the approver and privileged-MFA gates, and RFC 6238 TOTP with single-use hashed recovery codes — all on node:crypto, no dependency. Also the RSA-4096/AES-256-GCM diagnostics envelope, black-box incident logging, the fail-closed production guard, and every key material path. Nothing private here is ever committed, and no credential may live in DEFAULT_SETTINGS — see CLAUDE.md §0. The two request-boundary modules live here too: rateLimit.js holds the one bounded keyed counter every attempt tracker and abuse limiter shares, validation.js holds the runtime shape checker that both request bodies and validateSettingsPatch() run through, and cookies.js is the hand-rolled Cookie parse/serialise behind the HttpOnly session transport and its double-submit CSRF pair.",
      "match": [
        "backend/adminAuth.js",
        "backend/rateLimit.js",
        "backend/validation.js",
        "backend/cookies.js",
        "backend/cryptoHelper.js",
        "backend/blackBoxLogger.js",
        "backend/keys/*.pem",
        "backend/developer_doomsday_keys/**",
        "developer_blackbox_keys/**",
        "**.env.example",
        "backend/productionGuard.js"
      ]
    },
    {
      "id": "extensions",
      "lobe": "platform",
      "name": "Tenant Extension Surface",
      "role": "The hook dispatcher and drop-in surface that lets a tenant customise behaviour without forking. Deliberately never overwritten by an update.",
      "match": [
        "backend/extensions/**",
        "frontend/js/extensions/**",
        "docs/THIRD_PARTY_DEVELOPER_GUIDE.md"
      ]
    },
    {
      "id": "maintenance",
      "lobe": "platform",
      "name": "Backups & Reporting",
      "role": "The scheduled jobs: daily dated backups with 7-day pruning, and the emailed operational reports.",
      "match": [
        "backend/backupEngine.js",
        "backend/emailReporter.js"
      ]
    },
    {
      "id": "deploy",
      "lobe": "ops",
      "name": "Deploy & CI Pipeline",
      "role": "The owner's internal Dev → Sandbox → Live pipeline: PM2 ecosystem files, nginx template, the SSH deploy script, and the four GitHub workflows. Live is gated on a manual approval click.",
      "match": [
        "deploy/**",
        ".github/**"
      ]
    },
    {
      "id": "tests",
      "lobe": "ops",
      "name": "Test Suites",
      "role": "Assert-driven, no framework: billing math, helper integration, HTTP routes/auth, the money paths and Razorpay webhook, and the fail-closed production startup guard — all five run by npm test against temp data dirs, never backend/data/. Playwright end-to-end journeys live under tests/e2e/ and run separately (npm run test:e2e) because they need a browser binary. Anything touching money must be covered here before it is called done.",
      "match": [
        "backend/test_*.js",
        "test.bat",
        "backend/playwright.config.js",
        "backend/tests/**"
      ],
      "priority": 100
    },
    {
      "id": "dev-loop",
      "lobe": "ops",
      "name": "Local Dev Loop & Manifests",
      "role": "The scripts that start and restart the thing locally, plus the dependency manifests that define the budget in CLAUDE.md §0. Restart_Server.bat frees port 5000 before relaunching, which is the safe way to restart after a code change.",
      "match": [
        "Restart_Server.bat",
        "backend/package*.json",
        "frontend/package.json",
        ".gitignore"
      ]
    },
    {
      "id": "mobile",
      "lobe": "ops",
      "name": "Mobile Wrapper",
      "role": "A Capacitor shell around the same web frontend. Carries no business logic of its own and must never grow any.",
      "match": [
        "mobile/**"
      ]
    },
    {
      "id": "trackers",
      "lobe": "knowledge",
      "name": "Live Trackers",
      "role": "What is to be done and what was built. Checklists mark [x] only when verified; the ledger is index-style and points back at them.",
      "match": [
        "docs/LEDGER.md",
        "docs/*CHECKLIST.md",
        "docs/*ROADMAP.md",
        "docs/*PLAN.md"
      ]
    },
    {
      "id": "reference",
      "lobe": "knowledge",
      "name": "Reference Docs",
      "role": "Architecture, requirements, and the handover snapshot. ai_handover.md §0 is the first and usually only thing a fresh session should read.",
      "match": [
        "docs/ai_handover.md",
        "docs/BRD.md",
        "docs/credentials.md",
        "**README.md",
        "docs/archive/**",
        "docs/adr/**"
      ]
    },
    {
      "id": "agent-config",
      "lobe": "knowledge",
      "name": "Agent Operating Rules",
      "role": "The rules a session loads and the mechanism that enforces them: standing rules, the reasoning behind them, the guard hooks, the vendored skill, and this brain.",
      "match": [
        "CLAUDE.md",
        "docs/CLAUDE.md",
        "docs/FOUNDATION.md",
        ".claude/**",
        "docs/brain/**"
      ],
      "priority": 100
    }
  ],
  "links": [
    {
      "from": "pos-ui",
      "to": "pos-api",
      "label": "HTTP/JSON",
      "note": "The browser calls the Express router over the network. No AST extractor can see this edge — it is asserted by hand."
    },
    {
      "from": "customer-portal",
      "to": "pos-api",
      "label": "HTTP/JSON (session-scoped)",
      "note": "Same network hop, but every /api/customer/* call now carries a session established by customerAuth.js."
    },
    {
      "from": "licensing",
      "to": "updates",
      "label": "signed release registry",
      "note": "The licensing server both signs licenses and publishes the release registry the update engine polls. Two responsibilities, one process."
    },
    {
      "from": "updates",
      "to": "deploy",
      "label": "release zip",
      "note": "release_pipeline.js produces the artifact the tiered engine distributes; the CI pipeline deploys the owner's own instances. Different distribution paths for the same build."
    },
    {
      "from": "tests",
      "to": "pricing",
      "label": "asserts every formula",
      "note": "test_billing_math.js imports frontend/js/lib/ directly — the reason frontend/package.json exists at all."
    }
  ],
  "pathways": [
    {
      "name": "Boot",
      "steps": [
        "pos-ui",
        "pos-api",
        "licensing",
        "persistence"
      ],
      "note": "The UI boots, hits the API, which verifies the license signature and grace window before the terminal is usable."
    },
    {
      "name": "A sale",
      "steps": [
        "pos-components",
        "pricing",
        "pos-api",
        "persistence"
      ],
      "note": "Billing desk computes with the shared math helpers, posts to the router, which writes the partitioned annual sales file."
    },
    {
      "name": "A customer payment",
      "steps": [
        "customer-portal",
        "pos-api",
        "persistence",
        "maintenance"
      ],
      "note": "Portal collects, router records against the advances ledger, the backup job captures it that night."
    },
    {
      "name": "An update",
      "steps": [
        "updates",
        "licensing",
        "security",
        "persistence"
      ],
      "note": "Engine polls the registry, verifies the release signature against release_public.pem, snapshots to _rollback/, then applies."
    }
  ]
}
