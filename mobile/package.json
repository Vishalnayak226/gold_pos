{
  "name": "gold-pos-customer-app",
  "version": "1.0.0",
  "description": "Capacitor-wrapped native Android shell around the existing customer.html portal (v1 Android app — see mobile/README.md)",
  "private": true,
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android",
    "add:android": "cap add android"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
